# 贡献约定

## 开发原则

1. 服务端是唯一权威状态来源。
2. 规则包不得依赖网络、数据库、系统时间或 UI。
3. 所有等待玩家输入的流程必须显式写入 `MatchState`，不得依赖异步函数调用栈续算。
4. 新增或迁移一项规则时，必须同时增加场景测试，并标明旧代码或规则文档来源。
5. 客户端只提交命令，不提交计算后的游戏结果。
6. 私密信息必须通过玩家视图投影过滤，不能仅靠前端隐藏。

## 完成定义

一次规则迁移只有同时满足以下条件才算完成：

- 行为来源和争议裁定已记录；
- 正常路径、放弃、超时和重连路径均有测试；
- 状态可 JSON 序列化并可从快照恢复；
- 不向无权限玩家暴露手牌或可响应能力；
- PC 与 360px 宽移动端均可完成交互。

## 提交前检查

```bash
npm run format:check
npm run typecheck
npm test
npm run build
```
