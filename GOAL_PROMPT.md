# 正式持续目标命令

P10 通过后使用下列命令；当前仓库已经以相同目标运行，本文作为可复核、可再次启动的仓库副本。

```text
/goal 在当前仓库持续完成 xiaoyaoyou-gpt 的“本地六人标准包+凤鸣玉誓可完整游玩 MVP”，直到 GOAL_ACCEPTANCE.md 的每一项均有当前直接证据并且 npm run goal:preflight 通过。

开始或恢复时依次阅读 README.md、AGENTS.md、PREPARATION_CHECKLIST.md、GOAL_CONTRACT.md、PLAN.md、GOAL_ACCEPTANCE.md、PROGRESS.md、docs/migration-baseline/，并只读使用本机 reference/psd48-master/。该旧参考不得修改、移动、删除、提交或上传。

先完成 P01–P10；P10 前不批量迁移正式内容。P10 全绿后在同一目标内立即按 PLAN.md 进入正式 MVP，不等待确认。每轮只选前置全绿的最高优先级 ready 节点：固定 C#/SQLite/协议证据与失败场景，实现最小完整变更，运行快速和节点完整测试，检查规则差异、隐藏信息、恢复与测试有效性，更新收据和 PROGRESS.md，以绿色可回滚提交结束后立即继续。

固定范围：六名玩家、标准包+凤鸣玉誓、服务端权威 TypeScript monorepo、PC/手机浏览器、本地开发；不做观战、账号、排位、聊天、商城、LLM 玩家、最终商业美术或云部署。客户端只能消费玩家投影和 availableActions。

15 秒操作窗口：可放弃则超时放弃，强制选择按可重放种子从合法项选择；持续断联 60 秒后自动决策，断联不延长当前窗口。所有效果栈、响应、濒死、随机、超时、快照和恢复必须可序列化、持久化与重放，命令处理幂等、重复、过期、乱序和越权。

规则证据依次为可重放 C# 行为、C# 源码+SQLite+旧协议一致结论、仓库维护者决定、保守可重放的 provisional-autonomous 临时裁定。疑似旧行为缺陷不得静默修改；记录最小证据和影响后继续所有不依赖节点。

必须优先证明冰心诀/嵌套反制/全员放弃和原效果恢复、单人/多人濒死/救援/死亡/胜负、15 秒/60 秒、断联重连、响应窗口中服务重启和六份玩家视图隐私。UI 必测 1366×768 与 360×800，Playwright 每局六个独立 BrowserContext，失败保留 trace、截图、控制台、网络和服务日志。

不得用空测试、覆盖率或自述替代行为验收；同因失败第三次必须换方法。除同一外部阻塞连续三个目标回合出现、所有替代路径已尝试且无其它 ready 节点外，不得停止。只有 GOAL_ACCEPTANCE.md 全部通过才能完成。
```
