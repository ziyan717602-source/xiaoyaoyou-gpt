# xiaoyaoyou-gpt 正式开发前总清单

状态：执行中

这是正式开发前的唯一总清单。`docs/migration-baseline` 保留证据和细节，本文件决定还剩什么、什么时候可以启动正式 `/goal`。

## 执行约定

- 后续准备工作按本清单连续执行，不再逐项向维护者请示。
- 日常技术选择、工具替代、测试修复和 UI 微调由 Codex 在已确认边界内自主决定。
- 清晰的 C# 行为即使看起来奇怪，仍优先复现；无定义、互相矛盾或无法运行的部分使用保守、确定性的临时裁定，记入规则决策日志后继续，不中断 `/goal`。
- 未明确选择开源许可证前，公开仓库默认不添加开源 `LICENSE`；公开可见不等于授权他人再分发。
- 不自动执行与本地 MVP 无关的付费、发布、商业化、对外消息、云资源创建或工作区外删除。
- 真正的外部阻塞不能处理时，先继续所有不依赖节点；只在已无任何可推进节点时才标记 blocked。
- 维护者可随时查看结果，但不需参与日常开发、测试、选型和迭代。

## 状态标记

- `[x]` 已有可复核证据。
- `[ ]` 尚未完成；括号中标出已完成的部分。
- 一项只有在其“完成证据”真实存在时才能勾选。

## P00：产品边界、自治权与止损

- [x] `P00-01` 固定六名玩家，不实现 2/4 人变体。
- [x] `P00-02` 首个规则集固定为标准包+“凤鸣玉誓”。
- [x] `P00-03` 操作超时 15 秒；可选动作默认放弃，强制动作从合法项中用可重放随机选择。
- [x] `P00-04` 持续断联 60 秒后进入自动决策；断联不延长当前 15 秒操作时间。
- [x] `P00-05` 不做观战，本地验证后才考虑云部署。
- [x] `P00-06` 规则权威顺序、C# 异常处理和规则决策模板已建立。
- [x] `P00-07` 无人值守时允许“临时裁定+明确记录+继续工作”，不为日常歧义停下等待。
- [x] `P00-08` 列出不做范围：观战、排位、好友、商城、多地域、LLM 玩家和最终美术管线。

完成证据：[08-open-questions.md](docs/migration-baseline/08-open-questions.md)、[02-rule-authority.md](docs/migration-baseline/02-rule-authority.md)、[12-pre-development-readiness.md](docs/migration-baseline/12-pre-development-readiness.md)。

## P01：生成可公开的干净 Git 仓库

- [x] `P01-01` 仓库名称确定为 `xiaoyaoyou-gpt`。
- [x] `P01-02` 公开历史排除完整 `reference/psd48-master`，本机仍保留只读副本。
- [x] `P01-03` 对当前未推送历史创建 Git bundle、提交清单和参考目录校验值备份。
- [x] `P01-04` 在不删除本地参考文件的前提下，重建不包含旧参考包的公开 Git 历史。
- [x] `P01-05` 使用 `.gitignore`/本地说明让参考目录在本机可用但永不进入公开提交。
- [x] `P01-06` 验证 `git ls-tree -r HEAD` 不含旧参考目录，`git lfs ls-files` 不再引用旧二进制对象。
- [x] `P01-07` 扫描待公开历史中的密钥、凭证、用户路径、大文件、生成物和第三方文件。
- [x] `P01-08` 默认不添加开源许可证，在 README/NOTICE 中说明公开不等于授权再分发。
- [x] `P01-09` 创建 `ziyan717602-source/xiaoyaoyou-gpt` 公开远程，只推送干净分支/标签。
- [x] `P01-10` 在远程上复核文件清单、仓库体积和克隆可用性。

完成证据：备份校验结果、干净历史扫描报告、GitHub 远程地址和新鲜克隆验证收据。

## P02：建立旧 C# 规则证据链

- [x] `P02-01` 对旧源码、SQLite 数据库、DLL 和关键资源生成校验值清单。
- [x] `P02-02` 记录解决方案、项目引用、目标 Framework、缺失项目和外部程序集。
- [x] `P02-03` 读取 SQLite schema 与表数量，保存只读数据快照描述，不修改原库。
- [x] `P02-04` 优先用可固定版本的仓库内/隔离工具链提供 .NET Framework 4.0 引用程序集与 MSBuild。
- [x] `P02-05` 按 `PSDBase → PSDGamepkg/PSDClientZero` 只构建行为预言机必需的最小子集。
- [x] `P02-06` 提取旧 TCP 协议输入/广播/私密回复的可机器读取记录方式。
- [x] `P02-07` 至少生成“基本回合”、“冰心诀响应”、“濒死/救援”三条可重放黄金轨迹。
- [x] `P02-08` 验证旧程序在重复输入下的不确定性，为每条轨迹标记证据等级。
- [x] `P02-09` 如果时间盒内无法驱动最小对局，生成正式降级报告，切换为源码+数据+人工轨迹证据，不继续修复无关 WPF 项目。
- [x] `P02-10` 建立一条命令可执行的预言机或证据查询入口。

完成证据：工具链说明、构建日志、三条黄金轨迹，或经验证的降级证据报告。

## P03：建立标准包+凤鸣玉誓权威迁移目录

- [x] `P03-01` 定义机器可读目录 Schema 与稳定 `canonicalId` 规则。
- [x] `P03-02` 确认范围内全部角色及扩展包归属。
- [x] `P03-03` 确认范围内全部手牌、装备、特殊牌及实际张数/编号。
- [x] `P03-04` 确认范围内全部怪物、NPC 与事件。
- [x] `P03-05` 为每个条目关联数据库 ID/CODE、C# 绑定方法、协议代码和描述文本。
- [x] `P03-06` 为每个条目标记结算原语、其它内容、隐藏信息和 UI 选择依赖。
- [x] `P03-07` 对数据库、C# 和之前 `scope.md` 的名称/数量/包归属做交叉比对。
- [x] `P03-08` 所有不一致进入问题清单；可依证据自主解决的直接记录结论。
- [x] `P03-09` 为每个条目记录证据等级、规则状态、迁移状态和场景覆盖率。
- [x] `P03-10` 生成人类可读范围报告与机器可检查数据，确保 100% 的范围条目已映射或显式标记争议。

完成证据：目录数据、Schema、数量对账报告、依赖图和争议清单。

## P04：冻结新引擎必须承载的规则语义

- [x] `P04-01` 定义对局生命周期：房间、入座、开局、回合、阶段、结算和结束。
- [x] `P04-02` 定义命令、领域事件、效果实例、来源、目标、父子关系和稳定身份。
- [x] `P04-03` 定义可序列化续算、等待点、合法动作、确认、放弃和过期。
- [x] `P04-04` 定义响应窗口的优先权、轮转、重开、关闭和嵌套响应。
- [x] `P04-05` 定义冰心诀/类冰心诀的取消对象、代价、反制和原效果恢复位置。
- [x] `P04-06` 定义伤害、替换、加成、减免、应用后触发与批次顺序。
- [x] `P04-07` 定义单人/多人濒死检测、当前救援对象、救援轮转、恢复、死亡和重算。
- [x] `P04-08` 定义死亡后效果、队伍状态、胜负检查和同时结束情况。
- [x] `P04-09` 定义随机种子、牌堆洗切、强制随机选择和重放一致性。
- [x] `P04-10` 定义 15 秒操作计时、60 秒断联状态、超时命令与服务重启后的剩余时间。
- [x] `P04-11` 定义命令幂等、客户端序号、过期/重复/越权命令的拒绝语义。
- [x] `P04-12` 定义公开状态、队友状态、玩家私密状态、服务端秘密与侧信道禁止项。
- [x] `P04-13` 定义快照、事件日志、规则集/协议/持久化版本和迁移策略。
- [x] `P04-14` 建立覆盖上述语义的黄金场景，包含正常、放弃、超时、断联、重启、无目标和嵌套分支。
- [x] `P04-15` 所有无法由 C# 确定的语义都有临时规则决策，不留给实现代码猜测。

完成证据：[`contracts/semantics.contract.json`](contracts/semantics.contract.json)、[`docs/rules-semantics/`](docs/rules-semantics/)、[`docs/verification/receipts/p04-semantics-contract.md`](docs/verification/receipts/p04-semantics-contract.md) 和 `npm run semantics:verify`。

## P05：在实现前完成桌面/手机交互证明

- [x] `P05-01` 定义大厅、入座、准备、对局、重连和结算的用户旅程。
- [x] `P05-02` 定义六人座位、队伍、血量、装备、技能、当前回合和等待对象的信息层级。
- [x] `P05-03` 定义桌面 1366×768 基准布局及更大屏幕的伸缩策略。
- [x] `P05-04` 定义手机竖屏 360×800 基准布局，不将桌面画面等比缩小。
- [x] `P05-05` 定义由服务端 `availableActions` 驱动的情境操作托盘。
- [x] `P05-06` 用模拟数据实现可点击原型：自己回合、单/多目标、冰心诀、多人濒死、长文本和大量手牌/技能。
- [x] `P05-07` 原型覆盖等待他人、剩余时间、超时后果、掉线、重连、过期操作和网络延迟。
- [x] `P05-08` 按键点击区、字号、对比度、焦点、键盘操作、滚动和安全区具备可检查标准。
- [x] `P05-09` 使用可见浏览器完成桌面/手机关键流程，保存截图、操作轨迹和问题清单。
- [x] `P05-10` 用独立视角检查信息优先级、误触风险、过载和私密信息暗示，迭代到客观验收项全部通过。
- [x] `P05-11` 人工审美反馈只作为后续可选改进，不阻塞无人值守 `/goal`。

完成证据：[`contracts/ux-prototype.contract.json`](contracts/ux-prototype.contract.json)、[`docs/ux-prototype/`](docs/ux-prototype/)、[`docs/verification/screenshots/`](docs/verification/screenshots/)、[`docs/verification/receipts/p05-ux-prototype.md`](docs/verification/receipts/p05-ux-prototype.md) 和 `npm run ux:verify`。

## P06：三个可失败技术探针

- [x] `P06-01` 探针 1：效果→冰心诀窗口→取消/全放弃→恢复→濒死→救援，每个等待点 JSON 往返后一致。
- [x] `P06-02` 探针 2：六个独立客户端、六份投影视图、命令版本/幂等和“有人能响应”侧信道检查。
- [x] `P06-03` 探针 3：响应窗口中重启服务，恢复 15 秒/60 秒语义，超时和强制随机可重放。
- [x] `P06-04` 每个探针有独立工作节点契约、失败产物、种子和验证收据。
- [x] `P06-05` 探针不迁移批量真实卡牌，失败时可删除/重做，不背负已迁移内容。
- [x] `P06-06` 根据结果确认或否决 TypeScript 自研引擎路线，并记录最终 ADR。

完成证据：[`contracts/technical-probes.contract.json`](contracts/technical-probes.contract.json)、[`packages/engine/src/technical-probes.replay.test.ts`](packages/engine/src/technical-probes.replay.test.ts)、[`docs/adr/0001-custom-deterministic-engine.md`](docs/adr/0001-custom-deterministic-engine.md)、[`docs/verification/receipts/p06-technical-probes.md`](docs/verification/receipts/p06-technical-probes.md) 和 `npm run probes:verify`。

## P07：把测试、Bot、浏览器与故障证据变成一等产物

- [x] `P07-01` 提供快速检查命令：格式、严格类型、Schema、依赖边界和相关单测。
- [x] `P07-02` 提供完整检查命令：所有黄金场景、属性测试、集成、重放、E2E 和构建。
- [x] `P07-03` 建立引擎不变量和 fast-check 命令模型，失败可收缩为最小命令序列。
- [x] `P07-04` 实现 `FirstLegalBot`、`AlwaysPassBot`、`CounterHappyBot`、`RescueBot`、`SeededRandomBot` 和 `ProtocolChaosBot`。
- [x] `P07-05` 六玩家无头 Bot 长局保存种子、初始快照、命令日志和最小失败序列。
- [x] `P07-06` 在真实 HTTP/WebSocket 和临时数据库上验证六连接、幂等、断线、重启和六份私密视图。
- [x] `P07-07` Playwright 每局使用六个独立 BrowserContext，覆盖建房/入座/准备、冰心诀、多人濒死、超时、重连和手机操作。
- [x] `P07-08` 无头 E2E 失败时自动保存 trace、截图、控制台、网络和服务端日志。
- [x] `P07-09` 可见浏览器验收使用一名真实交互视角+五个策略 Bot，无需人工操作六窗口。
- [x] `P07-10` 建立 CI：每次提交快速检查，合并/里程碑执行完整测试，夜间运行长局 Bot/浏览器矩阵。
- [x] `P07-11` 所有工作节点都生成验证收据，不以行覆盖率或 AI 自述代替验收。

完成证据：[`contracts/verification-system.contract.json`](contracts/verification-system.contract.json)、[`docs/verification/p07-system.md`](docs/verification/p07-system.md)、[`docs/verification/receipts/p07-verification-system.md`](docs/verification/receipts/p07-verification-system.md)、[`.github/workflows/`](.github/workflows/) 和 `npm run check:full`。

## P08：用探针证据冻结生产架构契约

- [x] `P08-01` 原则已确定：服务端权威、确定性引擎、玩家视图投影、事件日志+快照。
- [x] `P08-02` 原则已确定：自研效果/响应状态机，不以 boardgame.io 为规则内核。
- [x] `P08-03` 原则已确定：TypeScript monorepo，引擎/协议/服务端/客户端严格分层。
- [x] `P08-04` 探针后固定 HTTP/WebSocket 框架、Schema 库、持久化库与数据库。
- [x] `P08-05` 冻结 `applyCommand`、`resume`、`projectPlayerView` 及错误/领域事件契约。
- [x] `P08-06` 冻结每局单线程 Actor/命令队列、定时器调度与优雅停止方式。
- [x] `P08-07` 冻结 WebSocket 消息包、版本协商、重连令牌、幂等键与错误分类。
- [x] `P08-08` 冻结事件序列、快照频率、原子提交、恢复与数据版本升级。
- [x] `P08-09` 建立威胁模型：越权命令、重放攻击、状态窥探、房间冒用、DoS 边界和日志泄密。
- [x] `P08-10` 定义日志、指标、对局/命令/effect ID 关联与隐私脱敏。
- [x] `P08-11` 提供一条命令启动本地六人环境，不需要云资源或手工配置秘密。
- [x] `P08-12` 更新 ADR，明确被拒绝方案、重新评估触发条件和不得跨越的包边界。

完成证据：最终 ADR、协议/Schema 契约、持久化契约、威胁模型和本地环境启动收据。

## P09：为无人值守 `/goal` 建立仓库级执行系统

- [x] `P09-01` 本机 `codex-cli 0.138.0` 中 `goals` 功能为 stable 且已启用。
- [x] `P09-02` 在根目录建立 `AGENTS.md`，固定规则证据顺序、包边界、测试要求、自主裁定和禁止动作。
- [x] `P09-03` 建立 `PLAN.md`，使正式 MVP 成为一个有依赖、可验证的工作图，不是松散 backlog。
- [x] `P09-04` 建立 `GOAL_ACCEPTANCE.md`，写明唯一最终停止条件与所有必跑命令。
- [x] `P09-05` 建立 `PROGRESS.md`，只保留当前检查点、已验证证据、下一节点和阻塞。
- [x] `P09-06` 建立稳定命令：`check:fast`、`check:full`、`test:replay`、`test:bots`、`test:e2e`和 `goal:preflight`。
- [x] `P09-07` 建立正式目标分支与提交策略：绿色节点才提交，失败产物保留，不通过检查不并入 `main`。
- [x] `P09-08` 建立无人值守规则：同因失败三轮后切换方法；可疑规则用临时决定继续；可选依赖缺失时使用降级路径。
- [ ] `P09-09` 试运行一个准备阶段小 `/goal`，验证自动续转、状态报告、暂停/恢复和完成判定。
- [x] `P09-10` 生成正式 `/goal` 命令，目标、非目标、必读文件、验证命令和停止条件齐全。

完成证据：`AGENTS.md`、`PLAN.md`、`GOAL_ACCEPTANCE.md`、`PROGRESS.md`、稳定命令和一份小目标试运行收据。

## P10：正式 Go/No-Go

- [ ] `P10-01` P00–P09 的所有硬项全部勾选，没有依靠“以后再补”才成立的核心契约。
- [ ] `P10-02` 迁移目录对范围内内容映射率为 100%，争议项全部有临时决定。
- [ ] `P10-03` 三个技术探针通过，且最终技术路线已根据证据冻结。
- [ ] `P10-04` 桌面/手机原型、六玩家隐私、重启恢复与超时证据全部通过。
- [ ] `P10-05` 新鲜克隆后能按 README 一键构建、测试和启动本地环境。
- [ ] `P10-06` `goal:preflight` 通过，正式 `/goal` 文本已准备，工作区干净并创建 pre-goal 标签。
- [ ] `P10-07` 生成一份准备总报告：已完成证据、临时决定、剩余非阻塞风险和回滚点。

仅当 P10 全部通过时，才启动正式开发 `/goal`。

## 不阻塞正式开发的后置工作

- 具体云厂商、域名、TLS、多区部署、Redis、队列和 Kubernetes。
- 玩家账号、好友、聊天、排位、成就、观战和玩家回放 UI。
- 最终卡图授权、高清资源、音频、复杂动画和多语言。
- LLM 玩家、通用规则 DSL、可视化规则编辑器和自治多 Agent 编排框架。
- 大规模性能优化、容量规划、商业化与运营工具。
