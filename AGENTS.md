# xiaoyaoyou-gpt 仓库执行规则

本文件适用于整个仓库。先读 `PLAN.md`、`GOAL_ACCEPTANCE.md`、`PROGRESS.md`、`GOAL_CONTRACT.md` 和相关工作节点证据，再修改代码。

## 不可越过的边界

- 固定六名玩家；规则范围只包含标准包和“凤鸣玉誓”。
- `reference/psd48-master/` 是只读本地证据：不得修改、移动、删除、跟踪、提交或上传。
- 不引入观战、账号、聊天、排位、商城、LLM 玩家、生产部署或未核权素材。
- 不把 boardgame.io 或旧 C# 服务端作为生产规则内核。
- 不在客户端复制、预测或裁定规则；可交互对象只来自服务端 `availableActions`。

## 规则证据顺序

1. 可稳定重放的 C# 黑盒行为。
2. C# 源码、SQLite 数据和旧协议的一致结论。
3. 仓库内已有维护者决定。
4. 满足确定性、隐私、实体守恒、无死锁和可重放约束的 `provisional-autonomous` 临时裁定。

疑似 C# 缺陷不得静默修正。把最小状态、命令、源码位置、观察结果、备选语义和影响写入规则决策记录；继续不依赖该问题的 ready 节点。

## 包边界

- `packages/protocol`：跨进程 Schema、消息、稳定标识和版本；不得包含规则。
- `packages/engine`：纯确定性状态推进；不得访问网络、数据库、文件系统、墙钟或未记录随机数。
- `apps/server`：房间 Actor、身份、连接、持久化、超时调度和玩家投影分发。
- `apps/web`：只消费玩家视图和 `availableActions`，负责响应式交互，不裁定结果。

所有等待点、效果、响应窗口、随机状态、超时决定和续算位置都必须是可 JSON 序列化的数据。命令必须处理幂等、过期、重复、乱序、越权和版本冲突。

## 工作节点循环

1. 只选择 `PLAN.md` 中前置已验证的最高优先级 ready 节点。
2. 先固定证据、失败场景、不变量、六份玩家视图和恢复预期。
3. 实现节点允许范围内的最小完整变更。
4. 运行 `npm run check:fast` 与节点专属完整测试；浏览器节点同时验证 1366×768 和 360×800。
5. 检查规则差异、隐藏信息、恢复、确定性以及测试是否真的覆盖失败模式。
6. 更新验证收据与 `PROGRESS.md`，只在绿色时创建一个可回滚提交，然后继续。

首次失败保存原始证据；同因第二次失败复查前提与测试；第三次改换方法。不得删除或放宽已审定断言来制造绿色。

## Git 与安全

- 正式目标工作在 `codex/goal-mvp`；`main` 保持可构建。
- 每个绿色节点一个提交，提交信息包含节点 ID；失败产物可以保留但不得声称完成。
- 不强推，不删除工作区外文件，不提交秘密、用户绝对路径、生成物或未核权第三方资源。
- 未选择许可证前不得添加 `LICENSE`；公开可见不等于授权再分发。
- 最终只有 `GOAL_ACCEPTANCE.md` 全部有当前证据且 `npm run goal:preflight` 通过，才能完成目标。
