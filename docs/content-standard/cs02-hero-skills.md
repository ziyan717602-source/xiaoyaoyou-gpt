# CS02 英雄技能迁移

CS02 按“归属图 → 通用机制 → 单个真实技能闭环”的顺序迁移，不先造一套无法证明正确的万能技能 DSL。英雄与技能的静态归属来自生成目录中每个英雄的 `dependencies.contentIds`，引擎内完整保存 34 名当前范围英雄、77 条技能归属边（76 个不同技能），并由 `hero-skills:contract` 逐边比对。

## 第一闭环：JN50402 剑匣

- 所属英雄：`xyy.hero.xj404` 慕容紫英。
- C# 的 `G0IS` 在装载原生技能时触发 `JN50402Action(type=1)`，执行 `TuxLimit += 2`；默认上限 3，因此最终上限为 5。
- `G0QR` 分支只负责在弃牌检查时显示技能，真正的裁决仍是“手牌数大于当前上限才弃牌”。
- 新引擎在六人选将全部完成时，根据权威 `heroId` 原子计算手牌上限；五张不弃，六张弃一。服务端状态和玩家投影公开上限，但仍只向本人公开手牌实体。
- 该被动没有选择、响应或超时窗口；其结果随状态快照、事件回放和 SQLite 恢复保持确定。

`JN50402` 已闭合；此时慕容紫英仍为 partial，因为 `JN50401` 赠剑尚未迁移。今后若某个已迁移规则能改变英雄或动态获得/失去技能，该节点必须复用技能派生属性重算后才能验收，不能只改客户端显示。

## 第二闭环：JN50501 凝冰焚炎

- 所属英雄：`xyy.hero.xj405` 玄霄。
- C# 的 `Valid` 要求批次中至少有一个以自己为目标的正数水/火项目；一旦触发，`Action` 会删除该批次内全部以自己为目标、没有 `IMMUNE_INVAO` 的水/火项目，包括同批的零值项目。只有零值项目时不触发。这个略显奇怪的批次行为按旧实现保留。
- 新引擎在替换、加减值完成并确定最终目标后、响应窗口开启前应用该被动。混合批次中只移除匹配项目，不影响其他玩家或雷/无属性伤害。
- 普通水火项目被移除后不开放隐蛊窗口；`IMMUNE_INVAO` 火伤仍进入正常隐蛊/冰心诀链，并能跨 JSON 检查点与 SQLite 重启继续。

`JN50501` 已闭合；玄霄仍为 partial，因为 `JN50502` 结拜及其战斗命中修正尚未迁移。

## 第三闭环：JN20202 拒绝

- 所属英雄：`xyy.hero.xj202` 苏媚。
- C# 先复用普通 TP01 的合法性判断，再只允许从自己的特殊类（`TuxType.TP`）手牌中选择一张作为冰心诀支付；普通技牌、战牌和装备均不合法。
- 协议用独立的 `play-skill-converted-reaction-card` 明确携带物理支付牌、技能 ID 和目标效果。服务端事件保留支付来源，子效果复用普通冰心诀的 `cancel-effect` 节点，因此反制、全员放弃、父窗口恢复和奇偶结算不另写一套状态机。
- 该动作只投影给当前优先持有者；15 秒超时仍是放弃，不支付手牌。JSON 检查点和 SQLite 重启可从转换后的子窗口继续。

`JN20202` 已闭合；苏媚仍为 partial，因为 `JN20201` 尚未迁移。

## 第四闭环：JN40301 烹饪

- 所属英雄：`xyy.hero.x3w03` 星璇。
- SQLite 同时登记 `TP02` 的行动模式与濒死模式；C# 要求从自己的手牌中选择任意两张，再把两张物理牌一并交给普通 TP02 处理。濒死触发的 `Valid(type=1)` 本身未重复检查手牌数，但输入器不足两张时没有合法选择，新引擎直接不开放动作，结果一致。
- 玩家投影只给原生星璇提供一个选择器：候选是自己的全部手牌、必选两张，避免按组合枚举造成动作数量平方增长。服务端仍校验恰好两张、互异、均在本人手牌中，不能伪造技能、目标或卡牌。
- 行动阶段生成与普通灵葫仙丹相同的 `card:xyy.card.tp02` 效果，因此可被冰心诀响应，支付来源和技能 ID 保留在事件中；濒死阶段复用既有救援优先级、治疗规划、装备修正和轮转，不开放额外旁路窗口。
- 两种使用都是可放弃操作：行动窗口超时结束行动，救援窗口超时 pass，均不会预先支付手牌。JSON 回放与六 WebSocket 的 SQLite 重启覆盖两张牌的原子支付和私密动作隔离。

`JN40301` 已闭合；星璇仍为 partial，因为 `JN40302` 尚未迁移。

## 第五闭环：JN20302 元灵归心术

- 所属英雄：`xyy.hero.xj203` 沈欺霜。
- SQLite 将其登记在自己的技牌阶段且不限制次数；C# 每次只允许选择一张 `TuxType.JP` 手牌和任意一名在场玩家，先弃置物理牌，再直接调用 `Cure(..., 2)`。没有 `G0CC` 卡牌效果消息，因此不开放冰心诀响应。
- 新协议使用窄的 `activate-hero-skill` 命令携带技能 ID、所选物理牌和目标；玩家投影给出本人的 JP 候选、所有存活目标及各自必选数量。服务端拒绝非 JP、外来/多张牌、伪造技能、越权角色和死亡目标。
- 弃牌与治疗属于同一个可重放事件；治疗沿用统一的 HP 上限和天蛇杖等修正。只要仍持有 JP 手牌，动作会继续开放，保持旧版“每丢弃 1 张”的可重复语义。
- 该技能是可放弃行动，15 秒超时结束行动而不支付手牌。JSON 回放、SQLite 重启和六 WebSocket 验证了支付、治疗结果与动作私密性。

`JN20302` 已闭合；沈欺霜仍为 partial，因为战斗相关的 `JN20301` 尚未迁移。

## 第六闭环：JN40401 饕餮

- 所属英雄：`xyy.hero.x3w04` 王蓬絮。
- C# 的候选来自 `ListOutAllCards()`，即自己的手牌、武器和防具；每次选一张，先发 `G0QZ` 弃置，再直接治疗自己 2 HP。技能未限制次数，只要仍有任意可支付牌就能继续发动。
- 玩家投影按手牌、武器、防具给出候选且固定目标为本人；服务端记录真实来源区域，拒绝外来牌、多张牌、伪造技能、错误目标或非本人行动阶段。
- 新引擎先构造移除支付牌后的状态，再调用统一治疗规划。这保留了关键顺序：若支付天蛇杖，本次只有基础治疗 2；若支付其他牌并保留天蛇杖，本次治疗为 3。支付防具同样先清空装备槽。
- 结算不开放冰心诀窗口；行动超时不支付牌。JSON 回放和六 WebSocket 的 SQLite 重启覆盖装备支付、HP、区域守恒与动作私密性。

`JN40401` 已闭合；王蓬絮仍为 partial，因为 `JN40402`、`JN40403` 尚未迁移。

## 第七闭环：JN50201 搜囊探宝

- 所属英雄：`xyy.hero.xj402` 韩菱纱。
- SQLite 将技能登记为自己的技牌阶段、每阶段一次；C# 要求本人至少有一张手牌且至少一名其他玩家仍有牌，随后选择一张本人手牌和【偷盗】或【铜钱镖】，通过 `G0CC` 进入普通卡牌结算。
- 玩家投影给出本人全部手牌作为单张支付候选，并分别投影 JP01 的有手牌对手、JP06 的可弃牌目标。JP06 的本人目标按支付后的状态判断，避免把唯一手牌既当支付又当可弃目标；服务端重新校验所有来源、模式、目标和原生归属。
- 转换事件同时记录物理支付牌、技能 ID 和虚拟卡牌 ID，物理牌立即进入弃牌堆。之后完全复用普通 JP01/JP06 的冰心诀窗口、不透明手牌槽、装备选择、确定性强制超时与效果恢复；冰心诀抵消后不会打开选牌，但本阶段次数仍已消耗。
- 当前回合序列化保存 `usedSkillIds`，服务重启后仍禁止再次发动，新回合才清空。行动阶段 15 秒超时属于未发动并且不支付牌。
- JSON 回放覆盖窗口和强制选择；六个真实 WebSocket 分别在响应窗口、隐藏选牌中重启 SQLite 服务，验证次数状态、目标装备、私密动作和不透明选项均正确恢复。

`JN50201` 已闭合；此时韩菱纱仍为 partial，因为 `JN50203` 尚未迁移。

## 第八闭环：JN50202 劫富济贫

- 所属英雄：`xyy.hero.xj402` 韩菱纱。
- SQLite 将其登记为自己的技牌阶段、每阶段一次；C# 先发送 `G0DH` 摸一张，再读取摸牌后的 `Tux.Count`，达到两张才发送一次强制弃牌。即使牌堆和弃牌堆都无法提供牌，阈值仍按实际手牌数判断。
- 玩家投影给原生韩菱纱一个零支付、零目标动作。服务端复用统一的确定性摸牌/弃牌堆洗回规划，并在同一个权威事件中记录摸牌结果、消耗本回合次数；摸后少于两张直接结束，达到两张则创建可序列化的本人强制选择。
- 选择只向本人公开全部当前手牌实例，恰好弃一张；其他五名玩家只能看到公开手牌数量和无私密牌 ID 的技能效果。该技能不产生卡牌效果或冰心诀响应窗口。
- 强制选择的 15 秒超时从合法手牌中使用可重放种子随机弃一张；未发动时的行动超时仅结束行动。JSON 回放和六个真实 WebSocket 分别验证了选择中重启、最终状态重启、次数状态与隐藏信息隔离。
- 旧 `G0DH` 会排除 `Board.ProtectedTux`。当前标准包+凤鸣玉誓范围内没有保护牌生产者；已知来源是范围外特殊英雄 SP301 的技能，因此本闭环只声明当前范围完整，不声称兼容特殊英雄扩展。

`JN50202` 已闭合；此时韩菱纱仍为 partial，因为死亡触发技能 `JN50203` 尚未迁移。

## 第九闭环：JN50203 盗墓

- 所属英雄：`xyy.hero.xj402` 韩菱纱。
- SQLite 将其登记在 `!G0ZW` 死亡批次、优先级 190、每次触发一次；C# 在优先级 100 先标记整批死亡和胜负，若韩菱纱也在本批死亡或本批已直接决定胜负则不会进入盗墓。否则它在优先级 200 的通用死亡弃牌前收取所有其他阵亡角色的手牌、武器和防具。
- 服务端把同时死亡作为一个可序列化批次：先完成全部救援和死亡标记，再一次性把所有阵亡牌区移入原生韩菱纱的手牌。装备收取后成为普通手牌；阵亡玩家的公开装备槽和手牌计数同时清空。没有牌的死亡批次仍走统一清理，但不触发技能。
- 收取后仅韩菱纱收到捕获牌实例、可分配动作和 15 秒期限。她可反复选择任意非空子集并交给任意其他存活玩家，每次分配重开 15 秒；也可随时结束并保留所有未分配牌。其他五席只看到公开手牌数量变化，不会从 `pendingChoice`、`availableActions` 或阵亡牌区看到捕获牌 ID。
- 可放弃窗口超时或断联自动决策均直接结束，不再随机分牌。无论分完还是提前结束，每个死亡批次只造成一次普通中性自身伤害；该伤害复用统一 TP03/FJ05 响应和伤害/濒死管线，因此可以被合法响应，也可以再次引发濒死批次。
- 旧 `G0HQ` 会排除 `Board.ProtectedTux`；与 JN50202 相同，当前标准包+凤鸣玉誓没有保护牌生产者，因此本闭环验证范围内的全部阵亡牌均可收取，但不外推到特殊英雄扩展。
- 单元场景覆盖单人死亡、两人同时死亡、本人同批死亡和死亡已决胜四种边界；JSON 回放覆盖分配、重启、系统超时及自伤响应；六个真实 WebSocket 覆盖私密动作、收取选择重启、部分分配、自伤窗口再次重启及最终牌区。

`JN50203` 已闭合；韩菱纱的基础身份和三个原生技能均为 verified。

## 第十闭环：JN50401 赠剑

- 所属英雄：`xyy.hero.xj404` 慕容紫英。
- SQLite 将其登记为自己的技牌阶段、可反复发动；C# 候选为本人装备区中的装备和其他在场角色，并用阶段内 `RAM.Present` 记录已经赠送过的目标。进入技牌阶段时全员 RAM 会清空，所以限制是“本技牌阶段同一目标一次”，而不是整局或技能总次数。
- 每次发动恰好选择一件当前装备和一名尚未获赠的其他存活玩家。权威事件先从慕容紫英装备槽移除该实体牌，再装入目标的同类槽；当前范围只有单个武器槽和防具槽，因此目标原有同槽装备立即进入弃牌堆。随后独立的统一摸牌事件补两张，保留“先替换、后摸牌”的旧顺序；牌堆不足时，新弃置的被替换装备可以进入确定性洗回并成为第二张牌。
- 阶段内的已赠目标集合保存在可序列化 `turn.usedSkillTargetIds`；SQLite 重启后同一目标仍被排除，换目标可以继续发动，新回合创建时该字段清空。只要没有装备或没有未用目标，动作就不再投影。
- 装备转移、替换弃牌和各玩家手牌数量是公开信息；补到的两张牌 ID 只进入慕容紫英本人的手牌投影。技能不创建卡牌效果或冰心诀窗口；15 秒行动超时属于未发动，不移动装备也不摸牌。
- 旧 `ListOutAllEquips` 还理解额外装备和假装备；当前标准包+凤鸣玉誓运行时只有物理武器、防具槽且没有假装备生产者，本闭环不声称兼容范围外特殊牌区。
- 单元测试覆盖同目标拒绝、换目标重复、伪造手牌/未知牌拒绝、行动超时、同槽替换和替换牌参与洗回；JSON 回放覆盖两次转移与目标历史；六个真实 WebSocket 覆盖首次转移、阶段中重启、重复目标拒绝、第二次转移、最终重启及五份补牌隐私投影。

`JN50401` 已闭合；慕容紫英的基础身份和两个原生技能均为 verified。

## 第十一闭环：JN40302 兄弟

- 所属英雄：`xyy.hero.x3w03` 星璇。
- SQLite 将其登记为自己的技牌阶段、每阶段一次；C# 的合法性检查把本人也计入“有手牌的同队角色”，发动后则只收取其他存活队友的全部手牌。星璇原有手牌保留并与收取牌一起进入后续分配候选，装备区、对手牌区均不移动。
- 旧 `G0HQ` 对每名队友执行整手转移并使用进程随机洗牌；该顺序既不影响所有权又无法稳定重放。新引擎明确采用座次顺序、再按各来源手牌顺序追加，作为保守且确定性的临时裁定；卡牌守恒与可选分配集合保持一致。
- 收取和阶段内一次标记在同一个权威事件中完成。仅星璇看到完整候选、分配动作与 15 秒期限；她可反复选择任意非空子集交给任意其他存活队友，每次成功分配重开 15 秒。其他五席只看到公开手牌数量变化，不会从选择、动作、日志或错误中得到牌 ID。
- 尽管描述写“分配所有手牌”，C# 的输入 `0`、`/0` 或空输入会立即退出循环，因此保留可随时提前结束并留下全部未分配牌的真实行为。超时或断联自动决策也直接结束；没有不可放弃步骤，不进行随机分牌，不创建卡牌效果或冰心诀窗口。
- 旧 `G0HQ` 会排除 `Board.ProtectedTux`；当前标准包+凤鸣玉誓没有保护牌生产者，因此本闭环收取当前范围内全部队友手牌，但不声称兼容范围外特殊英雄。JSON 回放覆盖部分分配和超时；六个真实 WebSocket 覆盖私密收取、选择中重启、部分分配、过期选择重启自动结束、次数状态与最终牌区恢复。

`JN40302` 已闭合；星璇的基础身份和两个原生技能均为 verified。

## 第十二闭环：JN10501 鬼灵精

- 所属英雄：`xyy.hero.xj105` 阿奴。
- SQLite 将其登记为自己的技牌阶段且不限制次数；描述中的“交给一名或多名玩家”由多次发动实现。C# 每次要求本人至少一张手牌并存在另一名存活队友，输入恰好选择本人手牌的一至全部非空子集和一名其他存活队友。
- 协议复用窄的 `activate-hero-skill` 命令；本人投影提供全部当前手牌、最少一张/最多整手和按座次排列的存活队友目标。服务端拒绝空集、重复牌、外来或未知牌，以及本人、对手、死亡或多目标输入。
- 权威事件从阿奴手牌移除所选实体，并按命令顺序追加到单个接收者手牌；不经过弃牌堆、装备区、效果栈或冰心诀窗口。没有阶段内次数标记，只要仍有手牌和存活队友即可继续发动。
- C# 的 `G0HQ` subtype 1 只把牌 ID 发给来源和接收者。新投影同样只让阿奴在发动前看到候选、让接收者在转移后看到本人新手牌；其余四席只看到公开手牌数量。15 秒行动超时属于未发动，不转移任何牌，随后按普通回合规则继续。
- JSON 回放覆盖连续两次向不同队友转移；六个真实 WebSocket 覆盖首次私密转移、SQLite 中途重启、非法对手目标、第二次转移、最终重启和六份牌面隔离。

`JN10501` 已闭合；阿奴在 `JN10502` 闭合前保持 partial。

## 第十三闭环：JN10502 万蛊蚀天

- 所属英雄：`xyy.hero.xj105` 阿奴。
- SQLite 将其登记为 `!R#BC`、优先级 `-150`、每阶段一次；C# `XJ405.cs:238-246` 只在阿奴进入自己的补牌阶段且手牌为零时有效，先令所有存活队友各摸一张，再令除阿奴外的所有存活角色各受一点伤害。`XIR.cs:622-630` 在 BC 阶段以优先级 100 执行普通补牌，因此技能完整结算先于本人的普通奖励摸牌。
- 引擎进入 `reward` 后先记录自动技能触发，再按座次为每名存活同队角色调用统一确定性摸牌规划。每张实体牌只进入其所有者的私密投影；其他五席只看到公开手牌数量。
- 随后的五目标中性伤害使用统一 `planDamageBatch`、TP03/FJ05 响应和多人濒死管线。冰心诀只移除其使用者自己的伤害项；其他目标继续受伤。响应、反制、15 秒放弃、救援、死亡和胜负重算均沿用现有机制。
- `turn.rewardContinuation` 序列化 `drawing-team` / `resolving-damage` 步骤及尚未摸牌的队友。只有伤害已启动、响应窗口/选择/濒死批次全部清空且对局未结束后，才记录恢复事件、执行阿奴本人的普通奖励摸牌一次，并进入弃牌或下一回合；因此响应或濒死中途服务重启不会提前摸牌、漏摸或重复摸牌。
- 非空手进入补牌阶段时完全跳过技能。单元测试覆盖空手/非空手、团队摸牌顺序、全体他人伤害、冰心诀局部抵消、濒死死亡和下一存活行动者；JSON 回放逐事件比较重启前后状态；六个真实 WebSocket 覆盖响应中重启、窗口恢复、三份团队手牌隐私、普通奖励延迟及最终续算。

`JN10502` 已闭合；阿奴的基础身份和两个原生技能均为 verified。

## 第十四闭环：JN20601 辣手摧花

- 所属英雄：`xyy.hero.xj206` 孔璘。
- SQLite 将其登记为自己的技牌阶段、可重复发动且优先级为零；C# `XJ405.cs:569-591` 要求孔璘当前体力至少为二，选择一名在场女性角色，并把本人和目标按此顺序交给一次普通 `Harm`。同一阶段已经记录在 RAM `harmed` 的目标不能再次选择；`XIR.cs:160-174` 在进入下一技牌阶段时清空 RAM。
- 本人投影只在 HP 至少为二且仍有未选择的存活女性时提供零牌、一目标动作。目标可以是队友或对手，但不能是本人、男性、死亡角色或本阶段已选角色；服务端同时拒绝空目标、多目标、额外卡牌、非原生技能和伪造事件伤害顺序。
- 权威激活事件先把目标写入可序列化 `turn.usedSkillTargetIds`，再以同一个确定性效果 ID 创建 `[孔璘一点中性伤害, 目标一点中性伤害]`。该批次完整复用 TP03、FJ05、FJ03、响应反制、濒死救援、死亡和胜负管线；任一角色的局部减免不会跳过另一条伤害。结算稳定后恢复孔璘的行动阶段，可继续选择另一名女性。
- 目标历史跨 JSON 与 SQLite 响应中重启保留，并在下一回合的新行动状态中清空。15 秒行动超时属于未发动，不记录目标也不造成伤害。技能不移动卡牌，因此六份投影只有动作归属不同，公开 HP 和伤害结果一致，不产生隐藏牌信息。
- 单元测试覆盖 HP 门槛、目标全集、非法输入、超时、事件效果 ID/顺序防篡改、换目标重复和 TP03 局部抵消；JSON 回放覆盖响应、目标濒死死亡和行动续算；六个真实 WebSocket 覆盖本人动作隔离、响应中 SQLite 重启、目标历史恢复、重复目标拒绝和第二目标保留。

`JN20601` 已闭合；孔璘在 `JN20602` 死亡变身闭合前保持 partial，非可选英雄魔尊 `XJ207` 及其技能仍依赖该变身节点。

## 第十五闭环：JN20602 生命献祭（当前运行时边界）

- 所属英雄：`xyy.hero.xj206` 孔璘；目标身份：不可在开局选择的 `xyy.hero.xj207` 魔尊。
- SQLite 将其登记为 `!G0ZW`、优先级 `-10`、每批一次且终止原死亡消息。C# `XJ405.cs:591-612` 在孔璘出现在死亡列表时，先从列表剔除本人，以 `G0OY,0` 卸载孔璘、以 `G0IY,0` 装载英雄 10207，再递归处理剩余死亡角色。因此它发生在普通死亡标记、卡区清理和胜负判定之前，不是死后重新加入对局。
- `G0IY` 的 change type 0 调用 `InitFromHero(reset=true)`，把角色恢复为存活、可选中、魔尊满 HP 5、基础战力 8、命中 2 和手牌上限 3；新技能归属由新的 `heroId` 自动得到。队伍、座次、连接、当前回合、手牌、武器和防具不变。
- 新引擎在全部普通救援机会失败后、`death.player-died` 之前记录 `death.hero-transformed`。事件只接受原生 XJ206 到 XJ207，并核对当前完整手牌与基础装备实体；英雄属性从权威目录派生，不能由事件或客户端伪造。变身者不进入 `deadPlayerIds`，其他同时濒死角色按原座次继续，最后才重算胜负。
- 若五彩霞衣或其他救援先把孔璘恢复到正 HP，仍保留 XJ206 且不触发变身。单元测试同时覆盖保牌保装备、事件目标防篡改、救援优先、多人死亡和双方最后角色同时归零时由变身改变胜负；JSON 回放覆盖每个救援检查点；六个真实 WebSocket 覆盖救援中与变身后的两次 SQLite 重启及手牌隐私。
- C# 描述还要求保留宠物并重载宠物效果。当前运行时在 `CS03-BATTLE-DECKS` 前没有宠物状态，无法制造非空宠物夹具证明守恒；因此本节点只把技能记录为 partial，绝不把“空集合自然保留”冒充完整验证。后续 CS03 引入宠物实体后必须补充非空宠物、效果重载、死亡批次和重启测试，才能将 JN20602 提升为 verified。

`JN20602` 的当前可达规则闭合并解锁魔尊身份；孔璘、魔尊和该技能仍因宠物保留边界保持 partial，`JN20701/JN20702` 可作为独立后续节点迁移。

## 第十六闭环：JN20701 蓄势待发

- 所属英雄：只能由 `JN20602` 变身到达的非开局可选英雄 `xyy.hero.xj207` 魔尊。
- SQLite 将技能登记为 `OCCURS=!R#ST`、`PRIORS=0`、`ONCE=1`、`TERMINI=0`，描述为本人回合开始阶段补充一张手牌。C# `XJ405.cs:616-619` 没有 `Valid`、输入或选择，直接发送本人抽一张的 `G0DH`；`XIR.cs:139-147` 在事件阶段前执行 `R<rounder>ST`，因此抽牌必须先于事件和行动。
- 权威回合推进先落盘 `turn.advanced`，把新活动玩家和可序列化阶段设为 `turn-start`；若新的存活活动角色原生拥有 JN20701，随后依次追加 `turn.hero-skill-triggered`、共享 `planDraw` 产生的一张确定性抽牌，再进入 `event` 与 `action`。一次推进只有这一条生成路径，不建立客户端命令、选择、响应窗口或超时决定。
- 抽牌复用普通牌堆耗尽后的确定性弃牌堆洗回、RNG 游标和实体守恒规则；事件 reducer 校验活动玩家、英雄归属、阶段、等待状态及精确抽牌计划，伪造触发玩家或抽牌结果不能改变回放。XJ207 不会出现在开局候选中，因此不需要为首个开局行动制造额外触发；变身后的下一次本人回合自然经过相同 `turn-start` 路径。
- 六份玩家投影都看到公开的技能触发与手牌数量增加，只有魔尊本人看到抽到的卡牌实例。单元测试固定 `turn.advanced -> trigger -> draw -> event -> action` 顺序、恰好一次、伪造触发拒绝和卡牌守恒；JSON 测试比较重复命令与完整事件归约；六个真实 WebSocket 验证本人私有抽牌、其他五席无身份泄漏，以及 SQLite 重启后仍处于同一行动状态且手牌不重复。

`JN20701` 已闭合；继续迁移同一变身身份的回合结束被动 `JN20702`。

## 第十七闭环：JN20702 崩坏

- 所属英雄：只能由 `JN20602` 变身到达的非开局可选英雄 `xyy.hero.xj207` 魔尊。
- SQLite 将技能登记为 `OCCURS=!R#TM`、`PRIORS=0`、`ONCE=1`、`TERMINI=0`，描述为本人回合结束阶段 HP 减一。C# `XJ405.cs:620-623` 没有 `Valid`、输入或选择，直接调用 `Harm(owner, owner, 1)`；`XIR.cs:631-642` 先执行 `QR` 弃牌，再执行 `TM` 技能，最后进入结束清理，因此触发点固定在奖励和强制弃牌完成后、下一回合推进前。
- 权威回合状态进入 `turn-end` 后自动记录 JN20702 触发，并创建一条来源和目标均为魔尊本人的普通一点中性伤害。它不直接改 HP，而是完整复用伤害响应：TP03、FJ03、FJ05、冰心诀反制链、濒死救援、死亡清理与胜负判断全部先结算；技能本身没有选择或超时，既有响应和救援窗口继续使用 15 秒默认放弃。
- 可 JSON 序列化的 `turn.turnEndContinuation` 在伤害响应、嵌套反制、濒死和服务重启期间保留。只有所有等待点清空后才记录 `turn.turn-end-resumed` 并推进一次；若本人死亡则先选择下一名存活玩家，若对局结束则绝不推进。reducer 同时核对原生技能归属、活动玩家、回合编号、确定性效果 ID 以及唯一自伤条目，伪造来源或目标无法污染重放。
- 单元测试覆盖有弃牌和无弃牌入口、普通扣血、TP03 抵消、自救、无人救援死亡、续算清理和事件防篡改；JSON 回放在每个响应 pass 后重启并比较完整事件归约；六个真实 WebSocket 验证仅本人可见的 TP03 响应动作、响应中 SQLite 重启、抵消后只推进一次以及最终重启不重复触发。

`JN20702` 已闭合；`XJ207` 的两项原生技能均已验证。XJ207 与来源 XJ206/JN20602 仍保持 partial 的唯一原因，是 CS03 引入宠物状态后必须证明非空宠物及其效果在变身中守恒。

## 第十八闭环：JN30201 追打

- 所属英雄：标准包可选英雄 `xyy.hero.xj302` 唐雪见；同英雄的 JN30202/JN30203 属于战牌阶段，继续等待 CS03。
- SQLite 将技能登记为 `OCCURS=G1TH`、`PRIORS=160`、`ONCE=1`、`TERMINI=0`。C# `XJ405.cs:626-644` 要求本人有手牌，原伤害结果至少包含一条实际正数、未带 `CHAIN_INVAO` 的 HP 减少；开启可选技能提示时还必须至少有一名受伤者不是本人。支付任意一张手牌后，对该结果中仍在场的不同受伤者按首次出现顺序各造成一点普通伤害，本人在同批与他人一起受伤时也包含在内。
- 旧引擎先在 `G0OH` 实际扣除 HP 并产生 `G1TH`，JN30201 的优先级 160 早于优先级 200 的濒死处理。因此新引擎记录实际正数 HP 减少后，先打开可 JSON 序列化的 owner-only 支付选择；只有放弃或完整结算所有嵌套追打后，才对当前全部零 HP 角色建立一次濒死批次。仅本人受伤、减免为零、目标此前已经零 HP 或 `CHAIN_INVAO` 条目均不制造错误触发。
- 支付卡先从本人私有手牌移入公开弃牌堆，再为稳定去重的目标生成普通一点中性伤害。该伤害完整经过 TP03、FJ03、FJ05、冰心诀反制、濒死、救援、死亡和胜负管线；若它再次造成符合条件的实际 HP 减少且本人仍有手牌，会形成下一次独立选择，每层都必须另付一张，因而不会产生无成本死循环。
- 选择允许空提交，15 秒超时和断联自动决策也默认放弃且不移动卡牌。支付选项及 `pendingChoice` 只投影给唐雪见本人，其余五席只见公开等待、手牌数量、弃牌和 HP 结果。reducer 核对原始效果 ID、稳定目标序列、当前手牌实例及规范追加伤害，伪造卡牌、来源、目标或数值均被拒绝。
- 单元测试覆盖私有支付、空提交、超时、本人单独受伤不触发、`CHAIN_INVAO`、同批本人加入、多层付费追打、致死前顺序和防篡改；JSON 回放在支付与每个响应 pass 后恢复；六个真实 WebSocket 在私有选择和嵌套伤害窗口各重启一次，并验证其余五席不含支付卡身份及最终状态不重复结算。

`JN30201` 已闭合；`XJ302` 保持 partial，剩余两项技能依赖 CS03 的完整战牌阶段。

## 第十九闭环：JN10401 气剑指

- 所属英雄：标准包可选英雄 `xyy.hero.xj104` 林月如；同英雄的 JN10402 在战斗失败后触发，等待 CS03。
- SQLite 将技能登记为武器导入 `!G1IZ` 和导出 `!G1OZ` 两个锁定触发，优先级分别为 120/70；C# `XJ405.cs:156-181` 按本人实际导入或导出的武器数量增减战力。当前运行时只有一个物理武器槽，所以首次装备是 `0→1` 加一、卸下是 `1→0` 减一、换武器是 `1→1` 不叠加。
- 引擎把装备区变化与技能派生属性放在同一个权威事件中：正常装备和替换、WQ04 典当、JN50401 赠剑、JP06 弃置装备、普通死亡清区及 JN50203 盗墓收取均使用相同的占用差值函数。防具变化不影响战力，普通英雄不应用修正。
- `strength` 与 `dexterity` 现在作为公开战斗属性进入所有玩家投影；JN10401 本身没有动作、选择、响应或超时。浏览器不需要推测装备加成，后续战斗直接消费服务端权威属性。
- 单元测试覆盖首次装备、替换、防具、典当、赠剑、铜钱镖和死亡清区；JSON 回放覆盖导入、替换、导出及 JP06 选择恢复；六个真实 WebSocket 覆盖赠剑替换后的公开战力和 SQLite 重启一致性，同时继续检查补牌隐私。

`JN10401` 已闭合；林月如保持 partial，唯一未迁移技能 JN10402 等待 CS03 的战斗胜负触发。

## 第二十闭环：JN10601 御剑术

- 所属英雄：标准包可选英雄 `xyy.hero.xj106` 酒剑仙；同英雄的 JN10602 会启动第二次战斗，等待 CS03。
- SQLite 将技能登记为武器导入 `!G1IZ` 和导出 `!G1OZ` 两个锁定触发；C# `XJ405.cs:249-276` 按本人实际导入或导出的武器数量发送 `G0IX/G0OX type 0`。`Player.cs:44-50` 与 `Pond.cs:21` 证明 DEX 是和怪物 AGL 比较的命中属性，对应当前权威字段 `dexterity`。
- 共享装备派生函数现在同时处理 JN10401 的 `strength` 和 JN10601 的 `dexterity`。只有原生技能持有者响应武器槽占用差值；首次装备加一、卸下减一、换武器和防具变化均为零。正常装备、典当、赠剑、铜钱镖和死亡清区继续走同一条事件路径。
- 单元测试固定普通英雄不加成、酒剑仙命中 `1→2→1`、换装不叠加、防具无影响和公开投影；JSON 回放覆盖装备、典当和逐事件归约。六连接场景在保留 XJ104 替换验证后，让 XJ106 接收第二把赠剑并在最终 SQLite 重启后保持命中 2、公开装备和私有补牌隔离。

`JN10601` 已闭合；酒剑仙保持 partial，唯一未迁移技能 JN10602 等待 CS03 的双战斗流程。

## 第二十一闭环：JN20102 不屈不挠 / JN30601 决斗 / JN30602 手下留情

- 所属英雄：标准包可选英雄 `xyy.hero.xj201` 王小虎与 `xyy.hero.xj306` 重楼。SQLite 将 JN20102 登记为每次本人掷骰后的可选触发；JN30601 是自己的技牌阶段可重复发动技能；JN30602 是决斗伤害的锁定修正。
- C# `XJ405.cs:881-924` 令重楼第 N 次发动支付 N 张手牌，选择一至两名其他在场角色，并严格按选择顺序处理每名目标：重楼掷骰、目标掷骰、低点方受三点伤害；平点时先对目标、再对重楼各结算两点。前一目标的响应、扣血、濒死、救援或死亡没有结束前，不开始后一目标。
- 所有 d6 结果由现有 `sha256-counter-v1` 随机流产生。每次权威事件记录并校验点数集合哈希、游标起止、结果、掷骰者、目标序号和重投次数；客户端只能提交服务端投影的技能动作或私有选择，不能指定点数、游标或派生伤害。
- 原生王小虎每次成为当前掷骰者且仍有手牌时，只向本人打开 JN20102 的零或一张手牌选择。支付后先把当前实体牌移入弃牌堆，再消耗下一次确定性骰点替换结果；仍有手牌时可以再次重投。空提交、15 秒超时或断联自动决策均接受当前点数，不支付牌。
- JN30601 的支付数量和本回合使用次数保存在可 JSON 序列化的回合状态中；同一回合下一次成本递增，新回合创建时清空。发动目标必须是命令顺序中的一至两名互异、其他、存活且可选中玩家，服务端拒绝外来牌、重复牌、错误数量、重复/非法目标和过期动作。
- 决斗伤害统一携带 `TUX_INAVO | ALIVE | RSV_DUEL`。因此冰心诀 TP03 不会被投影，但五彩霞衣 FJ05 等合法救援仍可进入通用伤害/濒死管线。`ALIVE` 在所有伤害修正后应用：起始 HP 至少二时保留一 HP，起始 HP 为一时仍会失去一点并进入救援或死亡；这与旧 C# `XIG.cs:1352-1363` 的实际实现一致。
- 决斗的目标序列、当前阶段、骰点、重投次数和伤害续算位置全部序列化。JSON 回放逐事件比较完整状态；六个真实 WebSocket 分别在两次不同玩家的私有重投选择中重启 SQLite 服务，再验证公开骰点、私有牌 ID、顺序伤害、次数和最终 HP 均不重复、不泄漏。

`JN30601` 与 `JN30602` 已闭合。`JN20102` 目前只对已经可达的 JN30601 骰点链完成验证；XJ201 的 JN20101 以及 CS03 后续所有骰点生产者必须复用同一重投门，才能把 JN20102 与 XJ201 提升为 verified。XJ306 仍因战牌阶段技能 JN30603 等待 CS03 而保持 partial。

## 后续迁移方式

每个技能继续沿用相同门槛：冻结 C#/SQLite 证据，列出触发点、所有选择和隐藏信息，先写失败场景，再实现服务端事件及玩家投影，完成单元、回放、Bot、六连接恢复和必要的浏览器流程，最后才更新逐项内容状态。
