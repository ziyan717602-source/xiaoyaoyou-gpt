# P05 UX 问题清单

| ID       | 发现方式           | 问题                                           | 处理结果                                                         | 状态   |
| -------- | ------------------ | ---------------------------------------------- | ---------------------------------------------------------------- | ------ |
| `UX-001` | 360×800 目标选择   | 自己席位被移动端隐藏规则覆盖，不能成为合法目标 | 调整选择器优先级；手机座位横条显示自己的摘要，底部重复席位仍隐藏 | closed |
| `UX-002` | 手机多人濒死截图   | 效果链出现原生横向滚动条，干扰当前救援层级     | 保留横滑能力并隐藏原生滚动条，节点仍明确显示排队顺序             | closed |
| `UX-003` | 浏览器控制台       | 热更新可能对同一容器重复 `createRoot`          | 在 `globalThis` 复用 React Root；连续两次刷新无新增错误          | closed |
| `UX-004` | 1366×768 视觉复核  | 卡牌说明和关键状态初版字号偏小                 | 正文/操作说明提高到 14px，非关键元数据不低于 12px                | closed |
| `UX-005` | 多目标误触反例     | 第三次点选可能让用户误以为替换既有目标         | 达到多选上限后保持原两项，确认区持续显示 `2/2`                   | closed |
| `UX-006` | 隐私与等待文案复核 | 指定“谁正在响应”会暗示其私密响应能力           | 等待状态只显示“其他玩家”，席位只展示公开状态和手牌数量           | closed |

客观验收问题为零。配色、字体气质和装饰强度仍可接受人工审美反馈，但不阻塞 P06 技术探针。
