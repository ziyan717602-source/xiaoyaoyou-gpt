# ADR-0003：事件日志与周期快照

- 状态：接受
- 日期：2026-08-18

## 背景

对局需要支持掉线重连、服务重启恢复、问题审计和确定性回放。只保存最终棋盘无法恢复正在等待的嵌套响应，只保存全部事件又会使长局恢复变慢。

## 决策

每个已接受命令原子写入领域事件与新版本，并周期性保存完整可序列化快照。恢复时加载最近快照并重放后续事件。

## 后果

- 状态结构需要显式版本迁移；
- 命令必须幂等并包含预期版本；
- 能复现复杂争议和生成回放；
- 需要设计日志保留、压缩与敏感信息访问策略。
