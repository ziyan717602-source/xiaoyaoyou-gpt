# 开放问题与决策日志

## 已确认的产品决策

| ID   | 决策          | 结论                                                       | 状态   |
| ---- | ------------- | ---------------------------------------------------------- | ------ |
| D-01 | 玩家人数      | 固定六人                                                   | 已确认 |
| D-02 | 首个规则集    | 标准包与“凤鸣玉誓”扩展                                     | 已确认 |
| D-03 | 规则冲突依据  | 默认复现 C#；交互开发可提问，无人 `/goal` 作临时裁定并继续 | 已确认 |
| D-04 | 操作超时      | 15 秒；可选动作放弃，强制动作随机选择合法项                | 已确认 |
| D-05 | 玩家掉线      | 断联满 60 秒后进入自动决策状态                             | 已确认 |
| D-06 | 观战          | 不提供                                                     | 已确认 |
| D-07 | 部署顺序      | 本地开发验证完成后再部署云端                               | 已确认 |
| D-08 | 仓库可见性    | 允许使用公开 GitHub 仓库                                   | 已确认 |
| D-09 | GitHub 仓库名 | `xiaoyaoyou-gpt`                                           | 已确认 |
| D-10 | 公开历史边界  | 排除完整 `reference/psd48-master`                          | 已确认 |
| D-11 | 正式开发执行  | 使用 `/goal` 持续开发、测试和迭代；不需日常人工介入        | 已确认 |

## 仍需确认

| ID   | 问题                 | 当前建议                                               | 状态   |
| ---- | -------------------- | ------------------------------------------------------ | ------ |
| Q-01 | 新项目源代码许可证   | 非阻塞；未明确选择前默认不添加开源许可证，保留全部权利 | 待确认 |
| Q-02 | 未来游戏素材权利范围 | 旧参考包排除后不阻塞代码仓库；使用图音/字体前仍需核验  | 待确认 |

## 已接受技术决策

| 日期       | 决策                                             | 依据                                                |
| ---------- | ------------------------------------------------ | --------------------------------------------------- |
| 2026-08-18 | 使用自研确定性规则引擎，不以 boardgame.io 为内核 | 嵌套响应、替换和可恢复续算超出简单 phase/stage 模型 |
| 2026-08-18 | 使用 TypeScript monorepo                         | 前后端共享契约，便于测试和逐步迁移                  |
| 2026-08-18 | 事件日志加周期快照                               | 支持审计、重放、重连和服务重启恢复                  |
| 2026-08-18 | 旧项目作为只读行为参考                           | 保留证据，避免生产系统继承阻塞线程与旧 TCP 架构     |
| 2026-08-18 | 工程方法采用“工作图 + 有界验证循环”提案          | 用依赖图控制范围，用可验证循环完成单个节点          |

这些技术决策的完整上下文记录在 `docs/adr`。

## GitHub 状态

- 本机 GitHub CLI 已登录账号 `ziyan717602-source`。
- 当前本地仓库没有配置远程地址，也没有进行推送。
- 仓库名已确定为 `xiaoyaoyou-gpt`。
- 维护者已确认公开历史排除完整 `reference/psd48-master`。
- 先备份并清理未推送的本地历史，再创建远程。
