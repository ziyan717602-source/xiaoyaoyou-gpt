# CS02 / JN20701 蓄势待发验证收据

日期：2026-08-21
状态：pass

## 规则证据

- SQLite：`OCCURS=!R#ST`、`PRIORS=0`、`ONCE=1`、`TERMINI=0`；描述为本人回合开始阶段补充一张手牌。
- `PSDGamepkg/JNS/XJ405.cs:616-619`：没有 `Valid`、输入或选择，`Action` 直接发送 `G0DH,<owner>,0,1`。
- `PSDGamepkg/XIR.cs:139-147`：执行 `R<rounder>ST` 回合开始技能后才进入事件阶段。
- SQLite 英雄表：XJ207 为不可开局选择的 XJ206 变身身份，原生拥有 JN20701/JN20702；当前可达性由已验证的 JN20602 运行时变身提供。

## 已验证边界

- 只有存活的原生 XJ207 成为活动玩家并进入本人 `turn-start` 时自动触发。生成顺序严格为 `turn.advanced`、JN20701 触发、抽一张、`event`、`action`；其他英雄和其他玩家不触发。
- 抽牌使用共享确定性 `planDraw`，继承牌堆、弃牌堆回填、RNG 游标和实体守恒规则。一次回合推进恰好生成一次触发和一次请求一张的抽牌，不建立命令、选择、响应窗口或超时。
- reducer 校验活动玩家、原生技能归属、`turn-start` 阶段、无其他等待点及确定性抽牌负载；伪造触发玩家会被拒绝。相同 JSON 初态和重复命令产生完全相同事件与最终状态，逐事件归约等于不中断结果。
- 所有玩家只看到公开手牌数量变化；抽到的实例 ID 只出现在 XJ207 本人投影，其他五份投影不含该身份。
- 六个真实 WebSocket 从确定性 XJ206/XJ207 变身后状态推进到下一回合，验证本人私有抽牌、其他五席隔离，并在 SQLite 服务重启后恢复同一行动状态和同一张手牌而不重复触发。

## 验证命令

- `npm test -- --run packages/engine/src/turn.test.ts`
- `npm run test:replay -- --run packages/engine/src/turn.replay.test.ts`
- `npm run test:integration -- --run tests/integration/damage-dying-lifecycle.integration.test.ts`
- `npm run check:fast`
- `npm run goal:preflight`

## 完整门禁矩阵

- 单元测试：83/83。
- 事件重放：36/36。
- Bot 长程模拟：6/6，其中伤害能力 Bot 连续完成至少 1,000 回合及一场完整对局。
- 六连接真实网络集成：15/15。
- Chromium 端到端：3/3，覆盖六个隔离 BrowserContext、五 Bot 房间旅程和移动端关键控件。
- 本地服务冒烟：6 个客户端，健康检查与根页面均通过。

## 内容状态

- `xyy.skill.jn20701`：verified。
- `xyy.hero.xj207`：partial；变身身份、基础属性和 JN20701 已验证，JN20702 与来源变身的非空宠物守恒仍待完成。
- `xyy.skill.jn20602`：partial；当前运行时变身边界不变，宠物守恒仍等待 CS03。
