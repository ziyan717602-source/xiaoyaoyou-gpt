# CS02 / JN40401 饕餮验证收据

日期：2026-08-19

状态：pass

## 规则证据

- SQLite：`OCCURS=R#GR`、`ONCE=0`，描述为技牌阶段每丢弃一张手牌或装备，自身 HP +2。
- `PSDGamepkg/JNS/XJ405.cs:1106-1124` 从 `ListOutAllCards()` 选择一张本人实体牌；执行顺序是先 `G0QZ` 弃牌，再 `Cure(player, player, 2)`。

## 已验证边界

- 只有原生王蓬絮在自己的行动阶段且仍有手牌、武器或防具时获得动作；投影候选覆盖三个区域，目标固定为本人。
- 服务端拒绝外来牌、多张牌、伪造技能和错误目标；事件记录权威来源区域，成功后恰好一个实体进入弃牌堆并清除对应装备槽或手牌。
- 先支付后规划治疗：支付天蛇杖时只治疗 2，支付其他牌并保留天蛇杖时治疗 3；均遵守 HP 上限。结算直接完成，不开放冰心诀窗口，剩余牌仍可再次发动。
- 15 秒行动超时不支付牌。JSON 回放在装备支付前后一致；六个真实 WebSocket 验证只有本人收到动作，HP 与装备区域在 SQLite 重启后保持。

## 失败复核

- 初始失败场景没有投影动作，命令返回 `forbidden`；实现后同一测试同时覆盖手牌支付和武器支付分支。
- TypeScript 首次指出命令验证后的卡牌变量仍可能为 `undefined`；在不改变运行时规则的情况下显式收窄已验证实体，严格类型检查恢复通过。

## 内容状态

- `xyy.skill.jn40401`：verified。
- `xyy.hero.x3w04`：partial，基础身份与 JN40401 已验证，JN40402/JN40403 仍待迁移。

## 回归计数

- 核心单元：62 条通过。
- JSON 回放：26 条通过。
- 策略 Bot：6 条通过。
- 六连接集成：7 条通过。
- Chromium E2E：3 条通过。
- 本地 smoke：6 个客户端通过。

回滚点：提交 `feat(CS02-6): add JN40401 self healing`。
