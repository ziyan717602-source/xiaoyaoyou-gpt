# CS02 / JN50202 劫富济贫验证收据

日期：2026-08-19

状态：pass

## 规则证据

- SQLite：`OCCURS=R#GR`、`ONCE=1`，描述为技牌阶段补一张牌，发动后手牌数大于等于两张则弃一张，每阶段一次。
- `PSDGamepkg/JNS/XJ405.cs:1356-1360` 先发送 `G0DH,<uid>,0,1` 摸一张，再按实际 `Tux.Count >= 2` 发送 `G0DH,<uid>,1,1` 强制弃一张。
- `PSDGamepkg/XIG.cs:1218-1330` 的 `G0DH` 摸牌支持牌堆补充；弃牌从当前合法手牌中必选指定数量。其 `Board.ProtectedTux` 排除规则的已知生产者 SP301 不在标准包+凤鸣玉誓范围，本收据不外推到特殊英雄包。

## 已验证边界

- 只有原生韩菱纱在自己的行动阶段且本回合未使用时获得零支付、零目标动作；服务端拒绝非原生角色、任意伪造支付/目标和重复发动。
- 激活先通过统一确定性规划实际摸一张，牌堆为空时可从弃牌堆种子洗回；摸牌结果、RNG 游标和本回合次数进入可重放事件。
- 摸后实际手牌少于两张不打开选择；达到两张则本人必须从全部当前手牌中恰好弃一张。没有冰心诀或其他响应窗口。
- 选择实体只投影给本人，公开效果栈不含摸到或原有的手牌 ID；其他五席没有可用动作，不能从选择或错误路径获知牌面。
- 15 秒强制选择超时使用共享的种子随机合法弃牌；行动超时未发动则不改变牌区。`usedSkillIds` 随快照保存并在新回合清空。
- JSON 检查点覆盖摸牌后的强制选择和随机超时；六个真实 WebSocket 覆盖选择中 SQLite 重启、本人选择、最终状态再次重启及五份私密投影。

## 失败复核

- 初始失败场景只投影 `end-action` 且激活被拒绝，证明技能未预先存在；实现后原断言转绿。
- 第一次复用选择结算时，事件校验仍只允许 JP01/JP06，测试在正常选择和超时两条路径同时失败；扩展同一校验与牌区解析到窄的 JN50202 效果类型后通过，没有放宽选择数量、归属或 RNG 断言。

## 内容状态

- `xyy.skill.jn50202`：verified。
- `xyy.hero.xj402`：partial，基础身份与 JN50201/JN50202 已验证，JN50203 仍待迁移。

## 回归计数

- 核心单元：65 条通过。
- JSON 回放：28 条通过。
- 策略 Bot：6 条通过。
- 六连接集成：7 条通过。
- Chromium E2E：3 条通过。
- 本地 smoke：6 个客户端通过。

回滚点：提交 `feat(CS02-8): add JN50202 draw discard`。
