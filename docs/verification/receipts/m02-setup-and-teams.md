# M02 选角、队伍与开局收据

日期：2026-08-19

状态：pass

## 验收范围

- 稳定房间座位与 seed 洗出的回合顺序分离；3v3 和先手确定。
- 34 名范围角色、30 名直接选池、六份三选一和一次确定性替补。
- 24 个普通卡牌条目展开为 56 个物理实体；六人各三张，余 38 张。
- `sha256-counter-v1`、拒绝采样、Fisher–Yates、seed 承诺和私密投影。
- 房间开始与 match 快照原子化；Actor、事件重放、内容指纹幂等和中途重启。

## 自动证据

```powershell
npm run setup:verify
npm run check:fast
npm run check:full
npm audit --audit-level=moderate
npm run goal:preflight
```

最终结果：

- M02 契约 10/10；纯 setup/属性与房间持久化测试 7/7；真实六连接 setup 重启轨迹 1/1。
- 200 个生成 seed 均满足 24 个候选/替补角色唯一、56 个卡实体守恒和 JSON 往返；同 seed 状态相同，不同 seed 改变开局。
- 六连接轨迹覆盖准备、开始、私有候选、替补、三人选定后服务重启、原命令幂等重试、同 ID 换内容/换玩家拒绝、余三人完成、3v3、先手和六份手牌隔离。
- 直接读取只读 C# SQLite 并与编译产物逐项对账：34 名角色名称/HP/战力/命中/性别差异 0；56 个卡实例及物理序列完全一致；角色投影 SHA-256 为 `f119eb616fff3e084e668d73ad7d8f0cc622bb8e29885829d4e1fb51e1806a97`。
- 全仓库单元/属性 14/14、重放 7/7、Bot 3/3、真实网络 4/4、Playwright 3/3、六连接烟测 6/6；npm audit 0 vulnerability。

实现过程中修正了两处验证到的漂移。第一，初版 RNG 使用域字符串和十进制 cursor，虽确定但不符合 P04 已冻结的长度前缀/uint64 规范；在形成持久数据前改为合同算法。第二，新增 `SEM-007` 后准备审计仍硬编码六项决定而失败；审计现验证连续 ID 及合同/决策文档一致，不再复制易漂移数量。产品断言未放宽。

## 明确不声称

角色技能、卡牌结算、正式回合、15 秒/60 秒自动化和最终 Web UI 仍属于后续节点；M02 的 `playing` 表示初态完成，不表示已能推进完整回合。
