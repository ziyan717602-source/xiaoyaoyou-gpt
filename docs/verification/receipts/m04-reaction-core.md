# M04 响应核心验证收据

日期：2026-08-19

状态：pass

## 验收范围

- 鼠儿果先支付、后响应、全员放弃后只结算一次；冰心诀支付后按稳定效果 ID 取消当前效果。
- 冰心诀反制冰心诀、父窗口恢复、旧放弃记录清空、优先权从反制来源下一席继续。
- 三层嵌套不依赖硬编码深度；结束效果从活跃栈尾裁剪，不留下永久停滞历史。
- 15 秒绝对截止时间只使用服务端接收时间；错误优先权、目标、卡牌和过期输入不改变状态。
- 六份玩家视图只向当前优先玩家投影自己的响应牌操作；窗口、续算和期限可持久化、重放和重启恢复。

## 已取得的节点证据

- 6 个反应核心单元场景覆盖全员放弃、单层取消、双层反制、三层嵌套、非法响应与服务端期限。
- 专用事件重放从鼠儿果前状态开始，穿过两个冰心诀和 JSON 子窗口重启检查点，最终逐事件重放、重启续跑与连续执行完全一致。
- 1,000 回合真实引擎 Bot 只消费玩家视图中的 `availableActions`，实际使用冰心诀并持续验证 56 张实体卡守恒。
- 真实 HTTP/WebSocket 六连接完成鼠儿果、第一张冰心诀、子窗口服务重启、重复命令幂等、第二张冰心诀反制、逐席放弃和原效果恢复。
- 网络场景逐版本确认只有当前优先玩家收到 `play-reaction-card`/`pass-reaction`，其他五份视图没有响应操作；重启前后窗口 ID、效果 ID、优先玩家和截止时间完全相同。

## 最终自动证据

```powershell
npm run reaction:verify
npm run check:full
npm audit --audit-level=moderate
```

- M04 契约 12/12；专项引擎/回放/Bot 8/8，专项真实六连接 1/1。
- 全仓库常规单元/属性 28/28、重放 9/9、Bot 4/4、真实网络 5/5、Playwright 3/3、本地六连接烟测 6/6。
- M01–M03 的房间、开局和回合合同继续全绿；公开历史扫描不包含 `reference/`、凭证、本机路径、大文件或生成物。

## 失败证据与修复

第一次专项网络测试在装载阶段因 `better-sqlite3` 只安装于 server workspace 而失败；改为从 server 已声明依赖加载。随后网络断言使用了回合顺序而非响应规则要求的座位顺序，导致预期优先玩家错误；改为按座位从效果来源下一席计算后通过。规则实现和断言范围均未放宽。

第一次整仓 `check:full` 在 M03 静态合同检查停止：M04 将确定性摸牌规划器从 `turn.ts` 提取到共享 `card-zones.ts`，旧验证器仍查找 `drawPlan`。验证器更新为检查共享 `planDraw` 后，从头重跑整仓全部通过。

## 回滚点

本节点提交信息为 `feat(M04): implement serializable reaction core`；可按该独立提交整体回滚。提交后继续运行 `goal:preflight` 并推送远端。
