# M03 回合核心验证收据

日期：2026-08-19

状态：pass

## 验收范围

- 八阶段可序列化回合图、无内容阶段显式通过、奖励摸牌、手牌上限和轮转。
- 鼠儿果目标摸二、武器/防具装备替换、弃牌堆回洗和 56 张实体守恒。
- 基础胜负、非法命令、六份投影视图隔离、事件重放、幂等与服务重启。
- 真实六人网络命令以及 1,000 回合策略 Bot。

## 已取得的节点证据

- 纯引擎 M03 场景 7/7；包含 40 个生成 seed、每个 30 步的守恒与 JSON 往返，并拒绝被篡改的摸牌结果和断裂的命令版本链。
- 200 回合事件日志从 M02 开局快照逐事件重放，与含 JSON 重启检查点的连续运行状态完全相同。
- 六人策略 Bot 只消费各自玩家视图中的 `availableActions`，完成 1,000 个真实回合并逐回合验证 56 张卡实体集合。
- 真实 HTTP/WebSocket 六连接完成开局、先手奖励摸牌、强制弃牌、下一玩家轮转、服务重启、私密视图恢复和旧命令幂等重试。
- match schema 从 v2 明确提升到 v3；单元场景证明 v2 补入回合、胜者、手牌上限和装备槽，未知版本拒绝加载。

## 最终自动证据

```powershell
npm run turn:verify
npm run check:fast
npm run check:full
npm audit --audit-level=moderate
```

- M03 契约 10/10，节点测试 9/9，真实六连接回合/重启轨迹 1/1。
- 全仓库常规单元/属性 22/22、重放 8/8、Bot 4/4、真实网络 4/4、Playwright 3/3、本地六连接烟测 6/6。
- 1,000 回合真实引擎 Bot 逐回合验证 56 张实体；200 回合日志完整重放等于含重启检查点的连续状态。
- `npm audit` 为 0 vulnerability；公开历史扫描没有 reference、凭证、本机路径、大文件或生成物。

## 失败证据与修复

第一次 `check:full` 的构建、重放、Bot、网络和 Playwright 均通过，最后的 `local:smoke` 失败。原因是本节点把架构 fixture 假牌改成合法 `CardInstanceId`（`xyy.card.local-private@N`），却漏改同文件仍搜索旧标记的隐私断言；修正断言后先单独验证六连接，再从头重跑整个 `check:full`，全部通过。规则断言和隐私范围没有放宽。

## 回滚点

本节点提交信息为 `feat(M03): implement deterministic turn core`；可按该独立提交整体回滚。`goal:preflight` 在提交后的干净工作树运行，结果由命令输出记录。
