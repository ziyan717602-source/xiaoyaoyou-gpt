# CS02 / JN20302 元灵归心术验证收据

日期：2026-08-19

状态：pass

## 规则证据

- SQLite：`OCCURS=R#GR`、`ONCE=0`，描述为技牌阶段每丢弃一张技牌，回复任意玩家 2 HP。
- `PSDGamepkg/JNS/XJ405.cs:542-565` 只筛选 `TuxType.JP` 手牌，再选择任意在场角色；执行时先发弃牌消息，随后直接 `Cure(..., 2)`，不经过卡牌效果消息。

## 已验证边界

- 只有原生沈欺霜在自己的行动阶段且持有 JP 手牌时获得动作；候选牌、必选一张、全部存活目标和必选一人均由服务端投影。
- 非 JP、外来/多张支付、伪造技能、错误阶段、错误行动者和死亡目标均拒绝；成功时物理牌进入弃牌堆。
- 治疗直接结算且不开放冰心诀窗口，基础值 2，并复用 HP 上限与天蛇杖加一；剩余 JP 手牌仍可再次发动。
- 可放弃的行动阶段超时不支付手牌。JSON 检查点前后事件回放一致；六个真实 WebSocket 验证只有本人收到动作，治疗与支付在 SQLite 重启后保持。

## 失败复核

- 首轮失败场景只投影结束行动，命令返回 `not-available`，证明主动技能能力尚不存在；实现后原断言不变并转绿。
- 网络重启后的 match 版本可能包含恢复期间的系统事件，因此断言保留“版本不得回退”并继续核对 HP、手牌和装备实体，而没有假定版本必须静止。

## 内容状态

- `xyy.skill.jn20302`：verified。
- `xyy.hero.xj203`：partial，基础身份与 JN20302 已验证，JN20301 仍待 CS03 战斗能力。

## 回归计数

- 核心单元：61 passed。
- JSON 回放：25 passed。
- 策略 Bot：6 passed。
- 六连接集成：7 passed。
- Chromium E2E：3 passed。
- 本地 smoke：6 个客户端通过。

回滚点：提交 `feat(CS02-5): add JN20302 direct healing`。
