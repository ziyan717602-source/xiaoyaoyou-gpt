# CS03-03C-03-OUTCOME 验证收据

日期：2026-08-28。结论：pass，限标准20怪物胜败结算引擎与恢复切片。前置回滚点 `4b6801a`；本收据所在绿色提交为下一回滚点。

## 直接行为证据

- 规则逐项来源见 `docs/content-standard/cs03-monster-outcome-evidence.md` 与 `contracts/monster-outcome.contract.json`。证据等级是 C#/目录一致结论，不宣称黑盒等价。
- `monster-outcome.test.ts` 的40个胜败分支执行实际战牌结果交接、真实伤害响应与捕获；逐人断言实际血量、归属、弃牌和实体守恒。另覆盖普通同属性换宠、GT04 PASSIVE 直接替换、GT01 防止伤害与冰心诀取消、GT03 多人濒死救一死一及冻结胜负恢复。
- `monster-outcome-effects.test.ts` 覆盖 GS02 伤害后重新选存活目标、GS04 匿名手牌获取、GL02 失去装备技能数值、GL03 先弃后重新计算并列最大值和并发收集、WQ02 治疗联动、GF03 全体先弃再摸以及真实怪物治疗来源。可选放弃不消费随机，也不生成零治疗事件。
- `immobilized.test.ts` 验证 GS01 的下一回合跳过事件、行动、遭遇、奖励和 TM 技能，超限时保留原15秒弃牌等待，完成后仅清一次定身并推进；拒绝伪造跳回合标记。
- `monster-outcome.replay.test.ts`：固定 seed30303、80组可收缩的怪物/胜负/选择序列，包含非空手牌、手动及系统超时；每次等待做 JSON 往返和领域事件重放，检查56手牌、46遭遇实体及六份私密视图。快照迁移还经过既有角色完整性校验。v13活跃战牌窗口升级到v14不改期限、来源、随机和等待位置。

## 真实网络恢复

`time-recovery-lifecycle.integration.test.ts` 新增三种模式。都经真实 HTTP 建房、六人准备/选角和六个 WebSocket；仅初始胜败场景写入测试 SQLite，此后不修改期限或规则状态。

1. GL03 失败：第一轮装备弃置、第二轮三个并列角色共享窗口和部分提交后分别重启；原期限不变，选择全部收齐才统一应用。
2. GT01 胜利：在真实防止伤害和濒死救援窗口重启；全放弃死亡后仍恢复被冻结的胜利。按 C# 无存活过滤的捕获路径处理，未悄悄更改规则。
3. GF02 胜利同属性换宠：在待选处重启并实际等待15秒，不挪动测试时钟；强制随机恰好执行一次，完成后重启不重复随机或宠物增减。

三种模式均从初始快照重放包括连接事件在内的持久化日志，得到与最终数据库快照完全一致的状态；其它玩家手牌不进入接收者投影。

## 验证结果

- `npm run monster-outcome:verify`：相关规则、重放、类型和14项时间恢复网络测试通过；随后新增跳回合篡改红测，修复后3项定身测试及下述最终全仓检查再次通过。
- `npm run check:full` 最终退出0：285单元、115重放、6 Bot、36集成、3既有原型 Playwright 测试，以及格式、严格类型、契约、构建和本地六连接烟测通过。原始输出：`artifacts/cs03-monster-outcome-check-full-final.txt`。
- `npm run oracle:inventory`：1558文件、7项目、12 SQLite对象未变；aggregate `c32a3e5e72e2b15da2891b8a04fe6856c030ac61e15345b4b196afc4e8a2df30`。
- 干净提交后、推送前的 `goal:preflight` 输出保存到 `artifacts/cs03-monster-outcome-preflight.txt`；不得把仅准备闸门通过当作最终 MVP 验收。

## 失败保留与独立检查

- 初始缺少效果解释器、运行时适配器及定身生命周期的红测均保留。类型检查修正可变临时数组、操作判别联合；错误的宠物 fixture 调用、卡牌实体编号和系统超时字段按既有真实接口修正，未放宽行为断言。
- 独立恢复检查新增6种选择篡改和3种换宠篡改，先确认失败后绑定真实所有者、规则选项、数量、属性和选择ID；再加入并修复普通行动阶段伪造跳回合标记。
- GL03 可选放弃原来会产生零值治疗；红测后修正为空选择不产生治疗。GF03 重洗、GL02 装备移除、GL03 支付及 GS04 随机转移都使用真实非空卡区，避免空 fixture 假通过。
- 怪物伤害走既有防止/反制/濒死/死亡后效果流程，所有父子来源和期限可恢复；后续目标在先前效果结束后重算。胜负只使用冻结结果，结算中的死亡或宠物属性不倒改胜负。
- 失败和修复输出保存在 `artifacts/cs03-monster-outcome-*`、`artifacts/cs03-immobilized-*`。没有修改 reference、生产死亡规则或削弱断言。

## 明确未完成边界

- 仅内部 `beginMonsterOutcome` 接收真实冻结结果，玩家不能提交胜负；正式回合入口仍等待整个 CS03 聚合闭合。
- 完成结算会交接战后摸牌数2（终局中止为0），尚未由正式回合实际消费该奖励；这是 CS03-03D 工作，不是完整回合完成。
- 战斗技能、额外参战、强制命中/胜败和宠物消耗仍属 CS03-03C-04；本节点不替代这些规则，也不提升目录条目为完整迁移。
- 现有3项 E2E 仅证明原型测试设施；新增胜败 UI、正式桌面/手机整局和最终干净克隆审计仍未完成。完整目标保持进行中。
