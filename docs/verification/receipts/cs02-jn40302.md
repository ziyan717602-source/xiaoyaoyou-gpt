# CS02 / JN40302 兄弟验证收据

日期：2026-08-19

状态：pass

## 规则证据

- SQLite：`OCCURS=R#GR`、`ONCE=1`，描述为自己的技牌阶段收取所有队友手牌，再任意分配全部手牌，每阶段一次。
- `PSDGamepkg/JNS/XJ405.cs:1063-1100`：合法性把本人也计入“有手牌的存活同队角色”；发动时用 `G0HQ` 收取其他存活队友的整手牌，随后反复选择本人当前手牌的任意非空子集并交给任意其他存活队友。
- 同一 C# 循环接受 `0`、`/0` 或空输入并立即结束，因此真实行为允许保留未分配牌；这优先于描述中的“分配所有手牌”。
- `PSDGamepkg/XIG.cs:1015+`：`G0HQ` 的手牌转移不向无关玩家公开牌面，并排除 `Board.ProtectedTux`。当前标准包+凤鸣玉誓没有保护牌生产者，本收据不外推到范围外特殊英雄。

## 已验证边界

- 只有存活的原生星璇在自己的行动阶段、本阶段未使用且任一存活同队角色（含本人）有手牌时获得零支付、零目标动作；发动即消耗本阶段次数。
- 权威事件按座次、再按来源手牌顺序原子收取其他存活队友的全部手牌，保留星璇原有手牌；不移动装备或对手手牌。旧版整手转移的随机排列不影响所有权且不可稳定重放，因此明确收敛为确定顺序。
- 星璇可重复选择任意非空子集交给任意其他存活队友，也可随时结束并保留余牌。服务端拒绝对手、本人、死亡目标、外来牌、重复牌、过期选择和重复发动。
- 每次分配重开 15 秒；可放弃窗口超时和断联自动决策均直接结束并保留余牌。技能不创建卡牌效果、效果栈节点或冰心诀响应窗口。
- 只有星璇收到手牌实例、选择和分配动作；其他五席只收到公开手牌计数。JSON 回放覆盖激活、部分分配和系统超时；六个真实 WebSocket 覆盖选择中重启、部分分配、过期选择重启自动结束、重复发动拒绝和最终状态再次重启。

## 失败复核

- 初始失败场景中，原生星璇只有普通出牌和结束行动动作，不存在 JN40302 激活、收取或分配路径，证明行为未预先实现；加入窄协议、权威事件和私密投影后原断言转绿。
- 首次接入后，通用选择路由无法识别多次“牌子集+目标”的命令和 pass 超时；为该序列化选择增加专用命令及超时处理，未放宽牌归属、同队目标、期限或隐藏信息断言。

## 内容状态

- `xyy.skill.jn40302`：verified。
- `xyy.hero.x3w03`：verified，基础身份和原生 `JN40301`、`JN40302` 均已闭合。

## 回归计数

- 核心单元：72 条通过。
- JSON 回放：31 条通过。
- 策略 Bot：6 条通过。
- 六连接集成：10 条通过。
- Chromium E2E：3 条通过。
- 本地 smoke：6 个客户端通过。

回滚点：提交 `feat(CS02-11): add JN40302 hand distribution`。
