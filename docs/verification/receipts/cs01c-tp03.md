# CS01C / TP03 隐蛊普通伤害模式验证收据

日期：2026-08-19

状态：pass（目录项保持 partial）

## 规则与状态边界

- C# `JP06.cs:523-558` 确认：当前伤害批次存在自己的正数且非 `HPEvoMask.TUX_INAVO` 项时可使用；结算时过滤该批次中自己的全部非 `TUX_INAVO` 伤害项。数据库文本明确“倾慕除外”。
- 伤害修正后、HP 变化前建立持久化 `damage-batch` 效果。每名具有可防正数伤害的目标都得到 15 秒优先权，不根据其是否持有隐蛊省略窗口；只有当前目标收到自己的隐蛊实体动作。
- 隐蛊支付后成为父伤害批次的技牌子效果并开放冰心诀响应。成功时删除自己的全部适用伤害项；被冰心诀抵消时恢复父伤害窗口并继续应用原伤害。`TUX_INAVO` 掩码不开放隐蛊窗口。

## 验证结果

- 单元测试覆盖 HP 延迟、玩家视图隔离、全部同目标伤害项移除、`TUX_INAVO` 排除、冰心诀抵消、15 秒超时默认放弃、牌张支付和濒死衔接。
- JSON 重启测试从父伤害窗口恢复，打出隐蛊并完成子响应；不中断状态、重启状态和完整事件重放得到相同终态。
- 六条真实 WebSocket 从天雷破常规响应进入仅目标可操作的伤害窗口，在此窗口重启服务端后保持窗口内容和优先者，再完成隐蛊及其子响应；其余五份视图没有可用动作。
- TP03 的复制、转移和流转模式依赖 FJ02 与后续角色技能，因此 `xyy.card.tp03` 保持 partial。TP04 与 ZP01–ZP04 依赖真实怪物牌堆/战斗生命周期，未实现假替代。
- `npm run check:full`：unit 45/45、replay 16/16、bots 6/6、integration 7/7、Playwright 3/3、本地烟测 6 个客户端；构建、类型、格式、公开历史与 56 张行动牌守恒检查全部通过。
- `npm run goal:preflight`、依赖审计和最终提交 SHA 在干净提交上复核；Git 回滚点见下方提交主题。

回滚点：提交 `feat(CS01C-1): add serialized TP03 damage prevention`。
