# CS01B / JP01 偷盗普通手牌模式验证收据

日期：2026-08-19

状态：pass（目录项保持 partial）

## 规则与隐私边界

- C# `JP06.cs:294-316` 确认：不能选择自己；目标必须仍可选且存在未受保护手牌；施放者面对匿名手牌槽选择一张，服务端再把对应真实卡牌转移给施放者。
- 浏览器协议只接收 `opaque-hand-slot-N`。`PendingChoice` 的选项和 continuation 不包含目标卡牌实例 ID；真实实例只存在于权威状态与内部结算事件。除目标查看自己的手牌外，其余五份玩家投影均不泄露这些 ID。
- 卡牌先支付并进入冰心诀响应。抵消后不创建选择；通过后创建不可放弃的 15 秒选择，超时使用持久化 RNG 随机。玩家已进入自动模式时，有效截止提前到选择打开时刻。

## 验证结果

- 单元测试覆盖动作投影、自身目标拒绝、匿名槽、错误玩家无动作、指定槽转移、冰心诀抵消、自动模式确定性随机与逐事件重放。
- JSON 重启测试在匿名选择窗口中恢复，提交同一槽位与不中断执行完全一致，完整事件流可重放到相同终态。
- 六条真实 WebSocket 在匿名选择窗口关闭并重启服务端，重连后保持相同选择；施放者看不到两张目标手牌 ID，选择第二槽后服务端精确转移第二张。
- `npm run check:full`：unit 43/43、replay 14/14、bots 6/6、integration 7/7、Playwright 3/3、本地烟测 6 个客户端；构建、类型、格式、公开历史与 56 张行动牌守恒检查全部通过。
- `npm audit --audit-level=low`：0 vulnerabilities；`git diff --check`：通过。
- 受保护牌的产生与保护集合属于 CS02 技能/效果切片，当前没有真实上游可验证，所以 `xyy.card.jp01` 只提升为 partial，不标 verified。

回滚点：提交 `feat(CS01B-2): add opaque JP01 hand transfer`。
