# CS02 / JN50201 搜囊探宝验证收据

日期：2026-08-19

状态：pass

## 规则证据

- SQLite：`OCCURS=R#GR`、`ONCE=1`，描述为技牌阶段将一张任意手牌当作【偷盗】或【铜钱镖】使用，每阶段限一次。
- `PSDGamepkg/JNS/XJ405.cs:1326-1354` 要求本人有手牌且至少一名其他玩家仍有牌；输入选择一张本人手牌及 JP01/JP06，随后通过 `G0CC` 进入普通卡牌结算。

## 已验证边界

- 只有原生韩菱纱在自己的行动阶段、仍有手牌、另一玩家仍有任意牌且本回合未使用时获得动作；服务端拒绝外来/多张/空支付、伪造技能、JP01/JP06 之外的虚拟牌和非法目标。
- JP01 只允许有手牌的存活对手；JP06 目标在扣除支付牌后仍必须有手牌或装备。物理支付牌、技能 ID 与虚拟牌 ID 都进入事件，支付恰好一次。
- 两种模式复用普通卡牌的冰心诀链、不透明手牌槽和装备选择。冰心诀抵消后不打开选牌，但次数已经消耗；强制选牌超时继续使用可重放种子随机。
- `usedSkillIds` 随当前回合序列化并在新回合清空；15 秒行动超时不视为发动、不支付牌。
- JSON 回放穿过响应窗口和强制选择；六个真实 WebSocket 分别在响应窗口、隐藏选择期间重启，只有本人看到支付牌与选择动作，SQLite 恢复次数标记和最终区域。

## 失败复核

- 初始失败场景只投影 `end-action`，证明技能并未预先存在；实现投影、协议、事件和次数状态后同一场景通过。
- 协议原先把转换支付固定为两张，放宽为一至两张后，JN40301 的“两张”约束保留在服务端语义层，协议测试改为拒绝空支付。
- 首次回放断言遗漏 JP06 所弃目标牌，并遗漏新回合的空 `usedSkillIds`；按真实事件结果修正期望，没有放宽规则断言。

## 内容状态

- `xyy.skill.jn50201`：verified。
- `xyy.hero.xj402`：partial，基础身份与 JN50201 已验证，JN50202/JN50203 仍待迁移。

## 回归计数

- 核心单元：63 条通过。
- JSON 回放：27 条通过。
- 策略 Bot：6 条通过。
- 六连接集成：7 条通过。
- Chromium E2E：3 条通过。
- 本地 smoke：6 个客户端通过。

回滚点：提交 `feat(CS02-7): add JN50201 card conversion`。
