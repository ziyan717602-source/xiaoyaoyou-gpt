# CS02 / JN20601 辣手摧花验证收据

日期：2026-08-21
状态：pass

## 规则证据

- SQLite：`OCCURS=R#GR`、`PRIORS=0`、`ONCE=0`；描述为本人 HP 至少二时选择一名在场女性，本人与目标 HP 各减一，同一回合不能再次选择相同目标。
- `PSDGamepkg/JNS/XJ405.cs:569-591`：`Valid`、`Input` 和 `Action` 只接受存活女性且排除 RAM `harmed`；`Harm(player, [player, target], 1)` 后把目标 UID 写入该集合。
- `PSDGamepkg/JNS/JNSBase.cs` 的 `Harm(IEnumerable)`：按传入顺序标记来源和目标，并发出一个普通伤害批次；没有免疫绕过标记。
- `PSDGamepkg/XIR.cs:160-174`：进入 GR 行动阶段时重置玩家 RAM，因此已选择目标只在当前行动阶段内保留。
- SQLite 英雄表：`XJ206` 可选且拥有 `JN20601/JN20602`；`XJ207` 不可选，须等待 JN20602 死亡变身节点解锁。

## 已验证边界

- 只有原生孔璘在本人行动阶段、HP 至少为二且仍有未选择的存活女性时看到零牌、一目标动作；女性队友和对手都合法，男性、本人、死亡、重复、空、多目标、额外卡牌和伪造技能均被拒绝。
- 激活先将目标记录到可序列化 `turn.usedSkillTargetIds`，再以一个 canonical 效果 ID 创建按 `[本人, 目标]` 排列的两条一点中性伤害。事件 reducer 同时校验效果 ID、目标历史和精确伤害负载，不能通过重排或篡改事件改变结算。
- 两条伤害复用统一 TP03/FJ05 响应、FJ03 修正、嵌套反制、濒死救援、死亡和胜负管线；TP03 只移除其使用者自己的项目，另一条仍继续。所有等待点清空后恢复本人行动，可继续选择另一名女性。
- 目标历史跨 JSON 与 SQLite 响应中重启保留，并在下一回合重置。15 秒行动超时属于未发动，不写入目标、不扣 HP；断联一分钟后使用相同超时决定。
- 六个真实 WebSocket 验证只有孔璘本人获得动作，响应中服务重启恢复同一窗口和目标历史，已用目标被拒绝且第二名女性仍可选。技能不移动卡牌，不产生新的隐藏信息。

## 验证命令

- `npm test -- --run packages/engine/src/turn.test.ts`
- `npm run test:replay -- --run packages/engine/src/turn.replay.test.ts`
- `npm run test:integration -- --run tests/integration/damage-dying-lifecycle.integration.test.ts`
- `npm run check:fast`
- `npm run goal:preflight`

## 完整门禁矩阵

- 单元测试：80/80。
- 事件重放：34/34。
- Bot 长程模拟：6/6，其中伤害能力 Bot 连续完成至少 1,000 回合及一场完整对局。
- 六连接真实网络集成：13/13。
- Chromium 端到端：3/3，覆盖六个隔离 BrowserContext、五 Bot 房间旅程和移动端关键控件。
- 本地服务冒烟：6 个客户端，健康检查与根页面均通过。

## 内容状态

- `xyy.skill.jn20601`：verified。
- `xyy.hero.xj206`：partial；基础身份和 JN20601 已闭合，JN20602 死亡变身及其依赖 XJ207 仍待迁移。
