# CS02 / JN20202 拒绝验证收据

日期：2026-08-19

状态：pass

## 规则证据

- SQLite：`OCCURS=&TP01&0`，描述为“任意特殊类手牌当做冰心诀使用”。
- `PSDGamepkg/JNS/XJ405.cs:443-462` 先调用普通 TP01 的 `Valid`，输入阶段只筛选 `TuxType.TP`，随后通过 `G0CC` 以所选物理牌支付并按 TP01 处理。

## 已验证边界

- 只有原生苏媚、当前响应优先、普通 TP01 合法且支付牌属于自己的 TP 类手牌时开放动作；普通技牌和伪造技能/外来牌拒绝且不改变状态。
- 支付牌进入弃牌堆，事件记录 `JN20202`，子效果使用与普通冰心诀相同的 `cancel-effect`，可被另一张冰心诀反制并正确恢复父窗口。
- 可放弃窗口超时仍按 15 秒 pass，不支付任何牌；其他五份玩家视图不泄露苏媚是否持有可转换牌。
- JSON 检查点前后转换、全员放弃和最终状态一致；六个真实 WebSocket 在转换后的子窗口重启，重复命令保持幂等，另一玩家反制后完整结算。

## 失败复核

- 首次实现把转换子效果错误标成卡牌 ID，反制后父效果停在 pending；改为现有规范 `cancel-effect` 节点，未新增旁路结算。
- 随后的网络重跑一度仍读取旧 `dist`；显式执行 TypeScript 构建后，同一网络断言通过，测试未放宽。

## 内容状态

- `xyy.skill.jn20202`：verified。
- `xyy.hero.xj202`：partial，基础身份与 JN20202 已验证，JN20201 仍待迁移。

## 回归计数

- 核心单元：58 passed。
- JSON 回放：23 passed。
- 策略 Bot：6 passed。
- 六连接集成：7 passed。
- Chromium E2E：3 passed。
- 本地 smoke：6 个客户端通过。

回滚点：提交 `feat(CS02-3): add JN20202 TP01 conversion`。
