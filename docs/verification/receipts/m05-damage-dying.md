# M05 伤害与濒死验证收据

日期：2026-08-19

状态：pass

## 范围与证据

- 14 项机器可读合同全部通过：天雷破的取消/通过、整批伤害原子写入、单人及多人濒死、灵葫仙丹救援、全员放弃、死亡清理、同批胜负/平局重算、错误优先权、六份私密投影、15 秒绝对期限、JSON 回放、救援窗口中重启、真实六连接幂等和长局 Bot。
- C# 证据定位：`JP06.cs:346-363`（天雷破）、`Procedure.cs:177-186`（0 体力存活检测）、`XIG.cs:166-383`（死亡、清理、胜负）、`JP06.cs:453-519`（灵葫仙丹救援 +2）。
- 黄金轨迹覆盖同批多人濒死：先救援一人，再对另一人全员放弃并死亡。旧 C# 的救援到达次序受并发影响，M05 延续 P04 的确定性规则，从当前濒死者开始按座次轮转；该临时边界已写入合同。
- M05 只实现灵葫仙丹的濒死救援模式；普通阶段对自己回复 2 点留给 `CONTENT-STANDARD`，没有伪装为已经完成整张卡牌。

## 验证结果

- `npm run damage:verify`：合同 14/14；专项核心、回放与 Bot 7/7；真实网络生命周期 1/1。
- `npm run check:full`：unit 33/33、replay 10/10、bots 5/5、integration 6/6、Playwright 3/3、本地烟测 6 个客户端；构建、类型、格式、公开历史检查全部通过。
- `npm audit`：0 vulnerabilities。
- 六玩家实战 Bot 在 1000 个真实回合中实际经过天雷破、冰心诀、灵葫仙丹、死亡与胜负，并在每条命令后检查 56 张牌守恒。
- 六条真实 WebSocket + 临时 SQLite 用例验证：救援窗口中服务重启精确恢复、重复放弃返回原始幂等回执、另一玩家救援、最终死亡清理和胜负。

## 失败先行记录

- 首次网络用例暴露辅助函数只接受 `playing`、未接受合法的 `setup` 阶段；修复测试驱动辅助函数，未放宽规则断言。
- 首次 Bot 用例在 `end-action` 后继续使用旧行动者；修复循环在回合切换后立即退出，未放宽规则断言。

回滚点：提交 `feat(M05): implement deterministic damage and dying core`。
