# CS01B / JP03 五气朝元验证收据

日期：2026-08-19

状态：pass

## 规则与实现

- C# `JP06.cs:330-335` 确认主效果选择施放者一方全部仍存活角色并各回复 1 点，治疗带水属性与技牌来源；`ContentRule.cs:189-196` 确认此牌可典当补 1。
- 主效果使用显式 `primary` 模式与完整、按座次排序的存活同队目标列表。服务端拒绝删减目标或混入敌方目标，支付后进入可持久化的冰心诀响应链；通过后逐人将 HP 增加 1 且不超过上限，抵消则无人回复。
- 典当使用显式 `pawn` 模式，目标列表必须为空；服务端将原牌放入弃牌堆后补一张，不创建效果栈或响应窗口。

## 验证结果

- 单元测试覆盖动作投影、伪造目标拒绝、成功群体治疗、敌方不受影响、冰心诀抵消和典当牌张守恒。
- JSON 重启测试在主效果响应窗口中恢复，后续每次放弃结果和不中断执行一致；逐事件重放得到相同终态。
- 六条真实 WebSocket 覆盖私有手牌不泄露、主效果通过后的三名队友治疗，以及典当不打开响应窗口并完成一换一。
- `npm run check:full`：unit 40/40、replay 13/13、bots 6/6、integration 7/7、Playwright 3/3、本地烟测 6 个客户端；构建、类型、格式、公开历史与 56 张行动牌守恒检查全部通过。
- `npm audit --audit-level=low`：0 vulnerabilities；`git diff --check`：通过。
- 本收据只把 `xyy.card.jp03` 提升为 verified；其余三张牌仍按合同中的依赖状态推进。

回滚点：提交 `feat(CS01B-1): implement JP03 primary and pawn modes`。
