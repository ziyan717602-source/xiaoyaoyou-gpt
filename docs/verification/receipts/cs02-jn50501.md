# CS02 / JN50501 凝冰焚炎验证收据

日期：2026-08-19

状态：pass

## 规则证据

- SQLite：`OCCURS=!G0OH`、`PRIORS=-40`，描述为水、火属性 HP 伤害无效。
- `PSDGamepkg/JNS/XJ405.cs:1625-1642` 的 `Valid` 以正数水/火项目触发；`Action` 随后删除以玄霄为目标、水或火、且没有 `IMMUNE_INVAO` 的全部同批项目，并未再次要求正数。
- C# 删除发生在伤害批次继续处理前，因此被免疫项目不进入后续隐蛊或濒死链。

## 已验证边界

- 原生玄霄遇到至少一个正数水/火项目时，同批的非绕过水/火项目全部移除（包括零值同类项目）；只有零值项目时不触发。雷属性不移除，`IMMUNE_INVAO` 水/火伤害不移除。
- 混合批次只移除目标为玄霄的匹配项目，其他玩家的项目保持原序与数值。
- 最终目标替换完成后按实际受伤者技能判定；无匹配项目时不产生伪响应窗口。
- `IMMUNE_INVAO` 火伤进入正常 TP03 响应链，JSON 检查点前后得到相同事件与最终 HP。
- SQLite 注入同一权威引擎边界后重启服务，六个真实 WebSocket 验证普通火伤无窗口且 HP 不变；绕过火伤只向合法持有者开放私有响应，全部放弃后 HP 正确减少。

## 内容状态

- `xyy.skill.jn50501`：verified。
- `xyy.hero.xj405`：partial，基础身份与 JN50501 已验证，JN50502 仍待迁移。

## 失败复核

首次六连接测试把注入响应窗口写成绝对时间 `1000`，重启调度器正确判定其早已超过 15 秒并自动放弃，连接后因而没有响应动作。修正仅把测试窗口的 `openedAt` 改为注入时当前时间；技能实现和“合法持有者必须看到 TP03 动作”的断言均未放宽。

## 回归计数

- 核心单元：57 passed。
- JSON 回放：22 passed。
- 策略 Bot：6 passed。
- 六连接集成：7 passed。
- Chromium E2E：3 passed。
- 本地 smoke：6 个客户端通过。

回滚点：提交 `feat(CS02-2): add JN50501 elemental immunity`。
