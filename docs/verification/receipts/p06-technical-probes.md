# P06 三个可失败技术探针收据

日期：2026-08-19

固定种子：`p06-fixed-seed-20260819`

## 结果

- Probe 1：冰心诀成功取消；双层取消恢复父伤害；每次输入前 JSON 往返；伤害后濒死并由救援恢复。两条分支均通过。
- Probe 2：六个内存隔离客户端分别派生视图，其他五人的手牌 ID 均为 `null`；只有当前优先玩家得到私密动作。接受命令幂等，越权猜测同形拒绝，过期版本不改变状态。
- Probe 3：截止前恢复保留剩余时间；截止后可选动作只放弃一次；强制选项稳定排序并用记录值重放；15 秒不因断联暂停，60 秒只切换未来窗口自动模式。
- 7 个探针测试全部通过，测试中没有标准包或凤鸣玉誓真实内容 ID。
- 失败产物路径预先固定为 `artifacts/failures/p06-probe-{1,2,3}.json`；该目录被 Git 忽略，绿色运行不生成伪失败文件。

## 验证命令

```powershell
npm run probes:verify
npm run check:fast
npm audit --audit-level=moderate
```

## 路线判定

ADR-0001 提升为“P06 技术探针确认”，继续 TypeScript 自研确定性引擎。此结论只确认显式效果/窗口/续算/投影/恢复模型，没有把探针代码升级为产品规则，也不替代 P07 的分层测试和 P08 的真实网络/持久化架构验证。
