# CS01B / JP06 铜钱镖普通卡区模式验证收据

日期：2026-08-19

状态：pass（目录项保持 partial）

## 规则与卡区边界

- C# `JP06.cs:365-412` 确认：选择任一仍可选且具有未受保护手牌或装备的玩家；目标可以是自己。对手手牌显示匿名项，装备公开；选择后对应手牌或装备进入弃牌堆。
- 对手手牌使用 `opaque-hand-slot-N`，自己的已知手牌使用 `own-hand:<实例 ID>`，公开装备使用 `equipment:weapon/armor`。服务端只接受当前选择快照中的项，并按权威卡区解析真实实例。
- 铜钱镖先支付并开放冰心诀响应；响应通过后才创建强制选择。15 秒超时与断线自动模式沿用 JP01 的确定性随机管线，选择、RNG 游标和卡区变更均可逐事件重放。

## 验证结果

- 单元测试覆盖自己/他人目标、匿名对手手牌、已知自己手牌、公开武器、防具保留、连续两张铜钱镖和 56 张行动牌守恒。
- JSON 重启测试在“匿名手牌 + 公开武器”的混合选择窗口中恢复，选择武器后的结果与不中断执行及完整事件重放一致。
- 六条真实 WebSocket 先弃置目标公开武器，再以第二张铜钱镖弃置匿名手牌；施放者始终看不到该手牌实例 ID。
- `npm run check:full`：unit 44/44、replay 15/15、bots 6/6、integration 7/7、Playwright 3/3、本地烟测 6 个客户端；构建、类型、格式、公开历史与 56 张行动牌守恒检查全部通过。
- `npm audit --audit-level=low`：0 vulnerabilities；`git diff --check`：通过。
- 审计把 JP01 测试夹具中不存在的 `JP05@9` 修正为目录中的真实物理实例 `JP05@10`，防止虚构序号掩盖牌张守恒问题。
- 受保护牌上游属于 CS02；因此 `xyy.card.jp06` 保持 partial，不标 verified。

回滚点：提交 `feat(CS01B-3): add mixed-zone JP06 discard`。
