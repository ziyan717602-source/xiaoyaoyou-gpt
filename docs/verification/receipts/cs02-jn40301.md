# CS02 / JN40301 烹饪验证收据

日期：2026-08-19

状态：pass

## 规则证据

- SQLite：`OCCURS=&TP02&0,&TP02&1`，描述为“任意2张手牌当做灵葫仙丹使用”。
- `PSDGamepkg/JNS/XJ405.cs:1037-1062` 的行动模式要求至少两张手牌，濒死模式要求存在零 HP 角色；输入器固定选择两张本人的手牌，随后通过 `G0CC` 交给 TP02 的对应模式。

## 已验证边界

- 只有原生星璇且至少有两张手牌时投影动作；投影提供本人候选牌与必选数量 2，不枚举所有组合，也不向其余玩家泄露可用性。
- 服务端拒绝重复牌、少于或多于两张、外来牌、伪造技能和错误目标；成功后两张物理牌原子进入弃牌堆并在事件中记录 `JN40301`。
- 行动模式复用普通 TP02 的可抵消效果栈，自疗 2；濒死模式复用当前救援目标、优先级、治疗规划和天蛇杖加成。
- 两种模式均可放弃；15 秒行动/救援超时不支付手牌。行动转换在 JSON 检查点前后得到相同状态；六个真实 WebSocket 在救援优先权交接后重启，重复旧命令保持幂等，且只有星璇收到转换动作。

## 失败复核

- 初始失败场景只看到结束行动或救援 pass，证明测试确实先于实现。
- 首次守恒回归显示 57 张牌，定位到测试夹具误写了不存在的 `zp01@20`；改为目录中的实体 `zp01@16` 后，同一 56 张实体守恒断言通过，未改引擎规则或放宽断言。

## 内容状态

- `xyy.skill.jn40301`：verified。
- `xyy.hero.x3w03`：partial，基础身份与 JN40301 已验证，JN40302 仍待迁移。

## 回归计数

- 核心单元：60 passed。
- JSON 回放：24 passed。
- 策略 Bot：6 passed。
- 六连接集成：7 passed。
- Chromium E2E：3 passed。
- 本地 smoke：6 个客户端通过。

回滚点：提交 `feat(CS02-4): add JN40301 TP02 conversion`。
