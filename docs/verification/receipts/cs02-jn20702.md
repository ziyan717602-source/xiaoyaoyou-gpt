# CS02 / JN20702 崩坏验证收据

日期：2026-08-21
状态：pass

## 规则证据

- SQLite：`OCCURS=!R#TM`、`PRIORS=0`、`ONCE=1`、`TERMINI=0`；描述为本人回合结束阶段 HP 减一。
- `PSDGamepkg/JNS/XJ405.cs:620-623`：没有 `Valid`、输入或选择，`Action` 直接调用 `Harm(player, player, 1)`。
- `PSDGamepkg/XIR.cs:631-642`：`QR` 完成奖励后的弃牌，再运行 `TM` 回合结束技能，最后进入 `IC` 结束清理。
- SQLite 英雄表：XJ207 是不可开局选择、由 JN20602 变身得到的身份，原生拥有 JN20701/JN20702。

## 已验证边界

- 只有存活的原生 XJ207 在本人回合奖励和强制弃牌全部完成后自动触发；事件顺序固定为进入 `turn-end`、JN20702 触发、确定性自伤，完整结算后才恢复回合结束并推进一次。技能本身没有客户端动作、选择或超时。
- 自伤是一条来源与目标均为本人的普通一点中性伤害，完整复用 TP03、FJ03、FJ05、冰心诀反制、濒死、救援、死亡清理和胜负管线。TP03 可只移除该条伤害；一 HP 本人可以被救回，也可以在全部救援放弃后死亡并由回合推进跳到下一名存活玩家。
- `turn.turnEndContinuation` 序列化正在结算伤害的续算位置，跨 JSON、响应链、濒死及 SQLite 重启保留；只有等待点全部清空后才追加 `turn.turn-end-resumed`，不会重复触发或提前推进。若伤害结束对局则先保持最终胜负而不推进。
- reducer 校验活动玩家、原生技能归属、回合阶段、技能每回合一次标记、由对局和回合号派生的效果 ID，以及唯一规范自伤条目；伪造效果来源或伤害目标均被拒绝。
- 六个真实 WebSocket 验证 TP03 动作只投影给持牌本人，其他五席不获得卡牌身份；服务在响应窗口中重启后仍接受原命令，抵消后恰好推进一次，最终再次重启也不重复触发。

## 验证命令

- `npm run test:unit -- --run packages/engine/src/turn.test.ts`
- `npm run test:replay -- --run packages/engine/src/turn.replay.test.ts`
- `npm run test:integration -- --run tests/integration/damage-dying-lifecycle.integration.test.ts`
- `npm run check:fast`
- `npm run check:full`
- `npm run goal:preflight`

## 完整门禁矩阵

- 单元测试：89/89。
- 事件重放：37/37。
- Bot 长程模拟：6/6，其中伤害能力 Bot 连续完成至少 1,000 回合及一场完整对局。
- 六连接真实网络集成：16/16。
- Chromium 端到端：3/3，覆盖六个隔离 BrowserContext、五 Bot 房间旅程和移动端关键控件。
- 本地服务冒烟：6 个客户端，健康检查与根页面均通过。

## 内容状态

- `xyy.skill.jn20702`：verified。
- `xyy.hero.xj207`：partial；变身身份、基础属性及 JN20701/JN20702 均已验证，唯一剩余边界是来源 JN20602 在 CS03 非空宠物状态中的守恒。
- `xyy.skill.jn20602`：partial；当前运行时变身边界不变，宠物守恒仍等待 CS03。
