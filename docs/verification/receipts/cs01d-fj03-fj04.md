# CS01D / FJ03 龙魂战铠与 FJ04 乾坤道袍验证收据

日期：2026-08-19

状态：pass（目录项保持 partial）

## 规则与掩码边界

- `PSDBase/Card/FiveElement.cs:17-39` 表明旧 `HPEvoMask` 是可组合位域；当前协议用固定顺序、不可重复的字符串数组保存全部十二个标志，避免来源、免疫与终止语义互斥。
- `JP06.cs:640-663`：FJ03 把持有者每个正数伤害项减 1，跳过 `TERMIN_AT/DECR_INVAO`，减到 0 的项删除；FJ04 删除持有者的正数 `FROM_JP` 项，`IMMUNE_INVAO` 绕过免疫。
- 两个被动在确定性伤害计划中、隐蛊响应窗口之前执行；因此 FJ03 后剩余的伤害仍可用隐蛊，FJ04 已删除的项不会泄漏虚假的隐蛊可用动作。

## 验证结果

- 单元测试覆盖 FJ03 普通减伤、`TERMIN_AT`、`DECR_INVAO`、减至 0 删除，以及 FJ04 的 `FROM_JP`、`IMMUNE_INVAO` 和非技牌来源。
- 天雷破现在显式携带 `FROM_JP`；真实卡牌结算验证 FJ04 保留 HP、防具和隐蛊手牌，且不打开伤害响应或濒死窗口。
- JSON 重启/事件重放在 FJ03 减伤后的伤害批次和濒死救援中保持一致。
- 比赛快照升级为 schema v6；v5 的 `normal/tux-inavo` 字符串和缺失的装备修正字段会在内存中显式迁移，v2-v4 也经过同一升级链，避免更新服务端后卡死旧 SQLite 对局。
- 六 WebSocket + SQLite 场景在原濒死窗口中途重启并完成 WQ02 救援，随后复用同一房间分别验证 FJ03 把天雷破 2 点减至 1、FJ04 完全免疫天雷破，最后继续完成死亡胜负流程。
- 第一次完整回归让战斗 Bot 在 1000 回合内完成对局数降为 0：固定策略持续攻击最低 HP 的 FJ04 持有者。保留失败断言后，策略改为优先用 JP06 拆除对方乾坤道袍，并在仍有其他目标时不向免疫者浪费天雷破；完成对局、伤害牌、救援牌和死亡计数断言均未放宽。
- 最终 `npm run check:full`：unit 51/51、replay 17/17、bots 6/6、integration 7/7、Playwright 3/3、本地烟测 6 个客户端；构建、类型、格式、公开历史和 56 张行动牌守恒全部通过。
- `npm audit --audit-level=low`：0 个已知漏洞；`git diff --check` 通过。
- FJ04 战力和两张卡的“装备失效”条件分别等待 CS03/CS02 的真实状态，不伪造布尔替代字段。

回滚点：提交 `feat(CS01D-3): add masked FJ03 and FJ04 passives`。
