# 验证收据：P02-PROTOCOL-SMOKE

- 节点：P02-06/08
- 日期：2026-08-19
- 结论：pass
- 证据等级：B（源码与真实 TCP 黑盒一致；旧随机结果不可重放）

## 执行结果

| 命令                                | 结果                                                                  |
| ----------------------------------- | --------------------------------------------------------------------- |
| `npm run oracle:protocol-smoke`     | pass；六个真实 TCP 连接、62 帧、9 个输入/输出家族；服务端 stderr 为空 |
| 连续运行 A/B                        | pass；帧族稳定，`H0SD` 座次与 `H0SL` 角色分配不同，证实旧随机不可重放 |
| 构建前后 `npm run oracle:inventory` | pass；聚合参考 SHA-256 未变化                                         |

首个成功原始轨迹 SHA-256 为 `0c4004380ba48d2f0419d16d8849bc57728294014b4ed6a5bb1a54996be448ae`。原始文件包含主机运行日志且位于被忽略的 `artifacts/oracle/`；公开证据只提交帧方法、消息家族、来源位置、观察结论与哈希。

## 剩余风险

- 当前脚本只驱动到随机选角公开，不是 P02-07 要求的完整基本回合、冰心诀和濒死/救援黄金轨迹。
- 旧程序的 UID 重排、随机选角和线程调度本身不可固定；后续轨迹必须区分稳定语义与非确定字段。
- 私密效果回复仍需在具体规则场景中证明，不能仅以握手的连接专属消息代替。
