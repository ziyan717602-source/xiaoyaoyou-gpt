# P01 公开远程与新鲜克隆验证收据

- 验证日期：2026-08-18
- 公开远程：<https://github.com/ziyan717602-source/xiaoyaoyou-gpt>
- 可见性：public
- 默认分支：`main`
- 验证提交：`765219dc3fe67860e29347c4019840a48e92e6aa`
- 新鲜克隆方式：从 GitHub HTTPS 地址执行 `git clone --no-tags`，没有使用本地对象捷径。

## 隔离检查

| 检查                           | 结果                         |
| ------------------------------ | ---------------------------- |
| `reference/psd48-master/` 路径 | 0                            |
| Git LFS 跟踪项                 | 0                            |
| 远程默认分支                   | `main`                       |
| 本地只读参考副本               | 保留，未删除、未移动、未修改 |

## 新鲜克隆命令

| 命令                                 | 退出码/结果                   |
| ------------------------------------ | ----------------------------- |
| `npm ci`                             | 0；120 个包审计，0 个已知漏洞 |
| `npm run format:check`               | 0                             |
| `npm run typecheck`                  | 0                             |
| `npm test`                           | 0；2 个测试通过               |
| `npm run build`                      | 0                             |
| 启动构建后的服务并请求 `GET /health` | 200；`status=ok`、协议版本 1  |

用于验证的临时克隆位于仓库外的备份根目录。自动清理由本机安全策略拒绝，因此暂时保留；它是公开远程的可重建副本，不属于项目工作树或 Git 历史。
