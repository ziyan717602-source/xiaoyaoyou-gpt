# 法务与授权检查

`reference/psd48-master` 中未发现能够覆盖整个旧项目的明确许可证文件。仓库还包含角色形象、卡图、背景、语音和音乐等二进制资源。

项目维护者已确认可以创建公开代码仓库。该决定表示项目层面的公开意愿，不自动证明其中每一项第三方代码和素材都具备再分发权。

在公开部署或商业化之前，仍必须确认：

- 旧版程序源代码的再发布与改编权限；
- 游戏名称、人物、卡图和说明文本的使用权限；
- 图片、音乐、音效和语音的网络传播权限；
- 第三方 DLL 与字体的许可证及分发条件。

旧历史曾跟踪约 1,084 个 LFS 二进制对象，包括图片、音频、DLL、数据库和文档。该历史已经在首次公开推送前备份并重建；当前公开分支与标签不包含这些路径或 LFS 对象。

项目维护者已确认采用下列公开边界：

- 公开仓库只包含新代码、自有文档和已核验可分发资料；
- 完整 `reference/psd48-master` 作为本地行为预言机，不进入公开 Git 历史；
- 未来要公开部署图片、音频、字体、DLL 或其它旧资料时，仍需逐项核验权利。

`reference/psd48-master/` 由仓库根目录 `.gitignore` 明确排除，本机副本保持只读。公开迁移证据只记录必要的相对来源位置、校验值和自行生成的行为轨迹。

本仓库目前没有开源 `LICENSE`。公开可见不等于授予复制、修改或再分发权；在维护者明确选择许可证前保留全部权利。
