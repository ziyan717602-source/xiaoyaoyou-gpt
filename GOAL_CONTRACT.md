# xiaoyaoyou-gpt 无人值守 `/goal` 执行契约

状态：准备阶段契约；P10 通过后生成最终目标文本

## 当前能力核对

- 官方定位：`/goal` 用于跨多个回合持续追踪一个可验证目标，适合代码迁移、大型重构、实验和游戏。
- 官方建议：目标必须大于一个普通回合，但不能是一组无关的开放 backlog；必须先定义成功条件、验证循环和停止条件。
- 官方能力：目标可持续多个小时，通过 `/goal`、`/goal pause`、`/goal resume` 和 `/goal clear` 查看或控制。
- 本机状态：`codex-cli 0.138.0`，`goals` 功能为 `stable true`。

官方参考：[Follow a goal](https://learn.chatgpt.com/use-cases/follow-goals)。

## 正式目标的形状

正式开发使用一个持久目标完成“本地六人标准包+凤鸣玉誓 MVP”，但必须由 `PLAN.md` 中的有向节点限定范围，不允许在运行时把无关功能加入目标。

最终命令骨架：

```text
/goal 完成 PLAN.md 中定义的 xiaoyaoyou-gpt 本地六玩家 MVP，不停止，直到 GOAL_ACCEPTANCE.md 的全部可验证停止条件通过。首先阅读 AGENTS.md、PLAN.md、GOAL_ACCEPTANCE.md、PROGRESS.md 和迁移基线。按依赖选择下一个 ready 节点，先固定证据/失败测试，再实现、验证、记录收据和提交，然后继续下一节点。不向用户请示日常技术、规则或 UI 选择；按 GOAL_CONTRACT.md 的自主裁定与止损规则继续。
```

实际启动前会将骨架中的“MVP”替换为精确的结束状态，不依赖“做得差不多”。

## 单节点自动循环

1. 从 `PLAN.md` 选择前置全绿且优先级最高的 ready 节点。
2. 阅读该节点的 C#/数据证据、规则决定和最小相关上下文。
3. 先写或锁定失败场景、不变量和六份玩家视图预期。
4. 实现最小变更，不扩张节点允许范围。
5. 运行快速检查；通过后运行节点完整场景、属性、重放、集成或浏览器测试。
6. 视觉节点使用可见浏览器检查桌面/手机；失败保存 trace、截图、日志和复现步骤。
7. 对差异、证据、隐私、恢复和测试有效性做一次独立检查。
8. 生成验证收据，更新 `PROGRESS.md`，以一个可回滚提交结束节点。
9. 立即继续下一个 ready 节点，不等待人工回复。

## 自主规则裁定

规则依据顺序：

1. 稳定可重放的 C# 黑盒行为。
2. C# 源码、SQLite 数据和旧协议的一致结论。
3. 已存在的维护者决策。
4. 无法确定时的保守临时裁定。

保守临时裁定必须同时满足：

- 确定性，可由种子和命令日志重放；
- 不泄露私密状态或利用响应窗口建立侧信道；
- 不克隆/丢失卡牌、角色或其它唯一实体；
- 不让效果栈、响应窗口或濒死批次永久停滞；
- 不改变已确认的产品范围；
- 生成 `provisional-autonomous` 规则决策、回归场景和受影响条目列表。

正式目标运行期间不因普通规则歧义请求用户回复。

## UI 无人验收

- 服务端 `availableActions` 是可交互对象的唯一来源，客户端不猜测规则。
- 桌面 1366×768 和手机 360×800 是每个视觉节点必测视口。
- 可交互控件必须通过点击区、遮挡、滚动、焦点、键盘、对比度和长文本检查。
- 冰心诀、多人濒死、放弃范围、超时后果和重连状态必须能从当前画面理解。
- 使用 Playwright 操作轨迹、截图差异、布局尺寸断言和独立视觉检查共同验收。
- 审美偏好无法完全自动化；运行中优先可理解、可操作和不误触，主观美术意见作为目标完成后的可选改进。

## 失败、重试与止损

- 首次失败：保存原始证据，定位失败层级，做最小修正。
- 第二次同因失败：重检前提、证据和测试有效性，不通过放宽测试变绿。
- 第三次同因失败：停止当前方法，记录诊断，使用备选实现或降级证据路径。
- 节点被外部工具/凭证阻塞：标记后继续其他 ready 节点。
- 只有当同一外部阻塞在至少三个目标回合内反复出现、且工作图中不再存在任何可推进节点时，才将目标标记 blocked。

## 范围与安全边界

目标可自主执行：

- 工作区内代码/文档/测试修改、本地 Git 分支与提交；
- 仓库内开发依赖安装、构建、测试、本地服务与临时数据库；
- 浏览器自动化、本地六客户端、Bot 长局与失败重放；
- 已确认公开仓库中的目标分支备份。

目标不执行：

- 删除本地旧项目原始参考、工作区外文件或未验证备份；
- 创建付费云资源、购买域名、改变账户或发布到生产环境；
- 对外发消息、开工单、发版、合并无关 PR 或修改他人系统；
- 公开 `reference/psd48-master` 或未核验权利的图片、音频、字体和 DLL。

## 进度和提交策略

- 每个节点一个可回滚提交，提交信息包含节点 ID。
- 快速检查失败不提交“已完成”；可保存诊断性 WIP 产物，但不合并到 `main`。
- 目标分支可按绿色检查点推送作为备份；所有验收通过前不快进 `main`。
- `PROGRESS.md` 不写长日记，只保留当前状态和下一 ready 节点，详细证据进入收据/测试产物。

## 唯一最终停止条件

正式 `/goal` 只在 `GOAL_ACCEPTANCE.md` 全部通过后完成。该文件至少要求：

- 标准包+凤鸣玉誓目录项 100% 已迁移或显式标记暂缓理由；
- 六玩家可从建房完成一局，包含冰心诀、濒死/救援、超时、断联/重连和胜负；
- 桌面与手机关键流程通过 Playwright 和可见浏览器验证；
- 所有规则场景、属性、六玩家 Bot、集成、重放、E2E、构建和干净克隆检查通过；
- 响应窗口中重启与不重启的最终状态一致，六份玩家视图无私密泄露；
- 临时自主规则决定、已知风险、回滚点和最终验证收据齐全。
